# 결과 — A: 채우는 값을 상수에서 앵커 값으로 (미결 23번), 2026.09.17

사전 등록: `PREREGISTRATION_filler.md` (커밋 `c672817`, 코드 전).
기준·예측을 결과 보고 고치지 않았다.

## 판정 — ✅ **합격**, 그리고 **짝 기준 넷이 전부 맞았다**

| | 기준 | 가-3 | **A** | 합격선 | |
|---|---|---:|---:|---|---|
| A·B | R1 · R2 | 0 · 0 | **0 · 0** | 0 | ✅ |
| C | R2′ 경계 숫자 | 0 | **0** | 0 | ✅ |
| D | 방향 오독 (사람이 읽음) | 7/30 | **5/30** | 악화 금지 | ✅ |
| E | 밴드값·등급 동일 | — | **0/80** | 80/80 | ✅ |
| F | 앵커 완결성 검사가 무는가 | — | **뭄** | 되돌려 확인 | ✅ |
| G | `pytest tests/ -q` | 565 | **567** | 통과 | ✅ |

| | 예측 | 결과 | |
|---|---|---|---|
| **P-1** | 방향 오독 **5건 이하** | **5건** | ✅ |
| **P-2** 🔴 | 79.2·125.8 은 **여전히 틀린다** | **둘 다 그대로** | ✅ |
| **P-3** | 등급 변경 0/80 | **0/80** | ✅ |
| **P-4** 🔴 | `follow_through` 아닌 문장은 **한 글자도 안 다르다** | **66/66 비트 동일** | ✅ |

## 🔴 P-4 가 이 회차에서 제일 값이 나갔다 — **판정은 재현된다**

앞 회차 결과에 「문장 생성이 흔들린다」고 적었다가 **같은 조건이 아니었음을
깨닫고 정정**했다(10.0 을 안 쟀는데 11 과 견줬다). 그래서 이번엔 **재 봤다.**

프롬프트가 안 바뀐 **66문장이 전부 비트 동일**하다. 🔴 **「흔들린다」는 말은
근거가 없었다.** 같은 프롬프트는 같은 문장을 낸다 — 앞선 회차들의 비교가
그만큼 단단하다는 뜻이기도 하다.

## 🔴 문장이 **없는 측정값을 말하고 있었다**

고쳐진 자리를 나란히 보면 상수 `10.0` 이 문장에 **그대로 나가고 있었다**:

| | |
|---|---|
| 가-3 | *"팔로스루도 **10프레임**만 지속되어 …"* · *"지속 시간 **10.0프레임**도 충분하나 …"* |
| A | *"추가 회전이 부족해 …"* · *"지속 시간 **5.0프레임**도 충분했으나 …"* |

즉 평가 표본의 문장들이 **재지도 않은 프레임 수**를 선수에게 말하는 모양이었다.
🔴 **제품에는 이 결함이 없다** — 서비스 경로는 실제 측정값을 넘긴다. 이건
**평가 계기만의 결함**이고, 그래서 고친 것도 `eval/` 뿐이다.

## D — 무엇이 고쳐지고 무엇이 남았나

**고쳐진 2건**: 굴곡 **4.8**·**6.2**(짧음 조각). 지속 시간이 앵커 값
(2프레임)으로 바뀌자 *"지속 시간이 **짧아** …"* 로 방향이 일관됐다.
앞 회차가 「계기 탓」이라고 지목한 **바로 그 둘**이다.

**남은 5건**:

| 자리 | 성격 |
|---|---|
| `inside/follow_through` **79.2 · 125.8** | ✅ **P-2 가 짚은 자리.** *"팔로스루가 **짧고** 방향을 유지하며 마무리됐다"* — **잘함 수준 문구를 앞에 베낀다** |
| `inside/trunk_lean` **29.8 · 32.2** | 조각 앵커를 줘도 안 고쳐진다. 가-3부터 그대로 |
| `inside/hip_rotation` **85.8** | 과회전인데 「부족」이라 한다 |

🔴 **남은 것의 성격이 하나로 모인다**: 전부 **옆 등급 문구를 끌어오는 것**이다.
그것이 **B**(가-3 의 ⑵ — 프롬프트에서 다른 등급 문구를 빼는 안)가 겨냥하는
자리다. **이제 계기가 곧으니 B 를 잴 수 있다.**

## 무엇을 바꿨나 — **`eval/` 과 앵커 하나**

| | |
|---|---|
| `football_evidence.py` | `setdefault(code, 10.0)` → **그 등급 앵커의 값**(`filler()`). 앵커가 여럿이면 가-3 과 같은 규칙으로 조각을 맞춘다. 값이 없으면 **조용히 넘기지 않고 멈춘다** |
| 루브릭 | 🔴 **가-3 에서 내가 더한 앵커가 두 번째 지표를 빠뜨렸다** — 채웠다(`inside_pass/follow_through` g1, 2프레임) |
| 검사 | 앵커가 `measured_by` 를 다 적었는가 (`test_an_anchor_lists_every_metric_the_criterion_measures`) |
| 🔴 안 건드린 것 | `src/` · 루브릭 **문구** · `bands` · 가중치 — **B-6 재실행 없음** |

## 🔴 이 표본이 이제 **못 재는 것**

두 번째 지표가 등급과 **어긋날 때** 문장이 어떻게 되는지는 이 표본으로 더 못
잰다. **일부러다** — 그 질문은 `second_metric.py` 가 따로 재고, 한 표본이 둘을
겸하게 두는 것이 바로 이번에 고친 문제다. **계기는 중립이어야 한다.**

## 기준선

🔴 **가-2·가-3 의 숫자는 `10.0` 위에서 났고 그대로 기록으로 남긴다.**
**이 회차부터가 새 기준선**이다(`evidence_filler_after.json`).
