# 결과 — 바깥이 짚은 자리가 얼마나 어긋나는가 (미결 `ho` 45번 6회차)

돌린 날 2026.09.18 · 사전 등록 [`PREREGISTRATION.md`](PREREGISTRATION.md)
(커밋 `1446d5c3`, **돌리기 전 커밋**) · 계기 `measure_margin.py` ·
원자료 `margin_raw.json`

🔴 **조사 회차다** — `src/`·`rubrics/` **0줄** · 새 상수 **0개**(1회차에서
import) · GPU 안 씀(축구 포즈 캐시, **1.6초**).

## 판정 — 기준은 **전부 통과**. 🔴 **그런데 이 회차의 값은 합격이 아니라 사후에 있다**

| 기준 | 내용 | 결과 | |
|---|---|---|---|
| **A** (계기) | ㉠ 부분집합 argmax — 창이 `e` 를 담고 양 끝 2 이상이면 창 안 argmax == `e` | 위반 **0** / 30 | ✅ |
| **B** (계기) | 관문 분모 전부 보고 · `other` = 0 | **0** / 102 | ✅ |
| **C** | 지시성 — 실제 `\|δ\|` 중앙 < 무작위 대조 중앙 | **10.0 < 14.0** | ✅ |
| **D** | 짝 기준 — 통과 ≥ 10편 · 큰 2편 빼도 C 유지 | **17편** · 빼면 **8.0 < 14.0** | ✅ |
| **E** | P1 · P3 보고 | P1 **참**(중앙 10 > 5) · P3 **거짓**(5.9% < 20%) | — |
| **F** | `src/`·`rubrics/` 무변경 · 새 상수 0개 | **0줄** | ✅ |
| **G** | `uv run pytest -q` ≥ 578 | **578 통과** | ✅ |

🔴 **그런데 사후가 C 의 통계를 무너뜨린다** — 아래 「사후 ⑴」. **클립 단위로는
9/17 로 동전 던지기**다. **그래서 아래 `m*` 를 「이만큼 주면 된다」로 내지
않는다.** 사전 등록 판정은 **고쳐 쓰지 않고** 그대로 둔다.

## 관문 — 분모부터

| | 관문 | 빠진 편수 |
|---|---|---|
| ① | 캐시 `error` | **0** |
| ② | `check_quality(leg)` | 🔴 **14** (`inside_pass` 6 · `instep_shot` 8) |
| ③ | `window=None` 로 `e` 를 얻음 | 1 |
| ④ | 공 커버리지 ≥ 0.30 | 2 |
| ⑤ | 터치 후보 ≥ 1 | 4 |
| | **통과** | **17 / 38** |

🔴 **②에서 14편이 빠진 것은 52번 1회차가 본 것과 같은 현상이다**(그쪽은 10편,
「표본이 강의 영상이라 상반신 컷이 섞였다」). **표본의 절반 가까이가 다리를
안 보여 준다** — 이 캐시가 원래 다른 질문(동작 분류)을 위해 뜬 것이기 때문이다.

## M1 — `δ` 의 분포 (n=17)

| | 중앙 | 평균 | 범위 |
|---|---|---|---|
| 실제 `\|δ\|` | **10.0** | 82.6 | 0 ~ **906** |
| 무작위 대조 | 14.0 | 54.9 | — |

**평균과 중앙값이 8배 벌어진다** — 아래 「사후 ⑶」의 두 클립 때문이다.

### 산수로 따라 나오는 값 — 🔴 **쓸 수 있는 값이 아니다**

`m* = |δ| 분위수 + 2` (㉡·㉢ 에서 따라 나온다. ㉢ 의 전제는 P3 이 확인했다).

| 커버 | `\|δ\|` | `m*` |
|---|---|---|
| 50% | 10 | **12** |
| 80% | 25 | **27** |
| 90% | 39 | **41** |
| 100% | 906 | 908 |

🔴 **이 표를 처방으로 쓰지 않는다.** 위 「판정」의 단서 때문이고, 100% 칸이
스스로 그 이유를 보여 준다 — **한 클립이 30초 어긋나 있으면 여백으로 덮는
것은 「클립 전체를 보라」와 같은 말**이다.

## M2 — 실효 여백 손실 (P3)

창 `[d-m, d+m+1)` 에서 `usable` 의 경계가 창 경계보다 안쪽으로 당겨진 정도.

| `m` | n | 당겨진 창 | 앞 중앙 | 뒤 중앙 | `ok` |
|---|---|---|---|---|---|
| 2 | 17 | 0.0% | 0.0 | 0.0 | 17.6% |
| 3 | 17 | 0.0% | 0.0 | 0.0 | 41.2% |
| 5 | 17 | 11.8% | 2.0 | 2.0 | 64.7% |
| 8 | 17 | 11.8% | 0.0 | 3.0 | 64.7% |
| 12 | 17 | 5.9% | 0.0 | 9.0 | 88.2% |
| 20 | 17 | 5.9% | 0.0 | 7.0 | 94.1% |

**P3 은 거짓이다 — 5.9%**(102창 중 6). 🔴 **내 예측이 틀렸고 방향이 좋은
쪽이다**: 창 안 검출 실패로 여백이 깎이는 일은 **거의 없고**, 그래서
**㉢(`m ≥ |δ| + 2`)의 전제가 이 표본에서는 대체로 성립한다.** 사전 등록에
「이 예측의 근거가 제일 약하다」고 적어 둔 그대로였다.

## 사후 관찰 — **판정 밖이다.** 위 표의 합격·불합격을 고쳐 쓰지 않는다

### 🔴 ⑴ C 가 쓴 통계가 **클립마다 다른 분포를 묶었다** — 네 번째다

클립 단위로 「실제가 무작위보다 가까운가」를 세면 **9 / 17** 이다. **동전
던지기와 구별되지 않는다.** 집계 중앙값이 이긴 것은 `δ` 가 작아서가 아니라
**무작위 대조군의 중앙값이 클립마다 5에서 322까지 흩어지기 때문**이다.

| 층 | n | `\|δ\|` 중앙 | 무작위 중앙 | 클립 단위로 이김 |
|---|---|---|---|---|
| 후보 ≤5 | 6 | 13.5 | **95.0** | 4/6 |
| 후보 6~9 | 2 | 17.5 | 21.5 | 1/2 |
| 후보 ≥10 | 9 | **7.0** | 11.0 | 4/9 |

**후보가 적은 클립은 `δ` 가 오히려 크고**(13.5) **무작위가 훨씬 커서**(95.0)
이긴다. **후보가 많은 클립은 `δ` 가 작지만**(7.0) **무작위도 작아서**(11.0)
절반만 이긴다. 🔴 **즉 C 의 승리는 「바깥이 잘 가리킨다」가 아니라 「후보가
적으면 무작위가 불리하다」에서 온다.**

🔴 **계기가 미끄러진 형태가 이번이 네 번째다** — 8회차(무엇을 재는가) ·
10회차(누구를 재는가) · 5회차(계기 검사의 기준선이 가설이었다) · **6회차(요약
통계가 비교 불가능한 것을 묶었다)**. 배운 것: 🔴 **대조군을 클립마다 따로
만들었으면 비교도 클립마다 해야 한다.** 클립별 대조군을 만들어 놓고 전체
중앙값끼리 견준 것이 이 회차의 실수다.

### ⑵ 파국 두 클립 — 여백 문제가 아니다

| 클립 | `δ` | 클립 길이 | 후보 | |
|---|---|---|---|---|
| `wnpIwUpws7g` | **+906** (30.2초) | 1,117프레임(37초) | 2 | `e`=39 · `d`=945 |
| `QbbsTcPS2to` | **−325** (10.8초) | 1,200프레임(40초) | 4 | `e`=676 · `d`=351 |

🔴 **바깥과 안쪽이 서로 다른 사건을 보고 있다.** 30초 어긋난 것을 여백으로
덮는 길은 없다. 둘 다 `instep_shot` 이고 후보가 **2·4개뿐**이라, 공이 거의
내내 잡히는데도(1.00 · 0.94) 접근 극소점이 몇 개 안 나온 클립이다.

### 🔴 ⑶ `e` 는 **정답이 아니다** — 이 회차가 재는 것의 한계

`e` 는 「알려진 이벤트」라고 적었지만 **정답 라벨이 아니라 안쪽(신전 각속도
argmax)의 자기 답**이다. 그래서 `δ` 가 크다는 것은 **바깥이 틀렸다**일 수도
**안쪽이 틀렸다**일 수도 있고, 이 회차는 **둘을 못 가른다.**

🔴 **52번 2회차가 열어 둔 `diagnose_impact.py`(「임팩트가 진짜 킥인가」)가
정확히 그 자리**인데 **52번이 보류**라 닫혀 있다. 위 파국 두 클립이 어느
쪽 잘못인지는 그것이 풀려야 안다.

### ⑷ production 이 빈 슬라이스에 `nanmean` 을 돌린다

`check_quality` 가 유효 프레임이 0인 클립(`UsnIlVhmIus`)에서 numpy
`RuntimeWarning: Mean of empty slice` 를 뿜고 **그 뒤에** `InsufficientQuality`
를 올린다. **결과는 안 바뀐다**(그 클립은 관문 ②에서 빠진다). 🔴 **고치지
않았다 — 조사 회차라 `src/` 를 안 건드린다.** 적어만 둔다.

## 🔴 이 회차가 **말하지 않는 것**

- **「여백 `m` 을 이만큼 주면 된다」** — 사후 ⑴ 때문에 `m*` 를 처방으로 안 낸다
- **드리블** — 표본은 슛·패스 드릴이고 **반복 터치는 여전히 0편**이다
- **제품 성능** — 캐시가 `max_seconds=40` 으로 떠 있고 제품 분석 창은 10초다
- **바깥과 안쪽 중 누가 틀렸는가** — 사후 ⑶

## 다음 회차의 질문

🔴 **처방으로 가지 않는다. 계기부터 고친다.**

1. **클립별 대조**로 다시 낸다 — 클립마다 `δ` 와 그 클립의 무작위 분포를
   견주고(예: `δ` 가 그 클립 null 분포의 몇 분위인가), **클립을 하나의 표로
   묶지 않는다.** 이 회차의 자료로 바로 된다(새 측정 불필요)
2. 그 위에서 **후보 수를 층으로** 두고 본다 — 사후 ⑴ 의 층이 사전 층이 된다
   (🔴 **같은 표본에서 사후를 사전으로 승격하는 것**이라, 그렇게 한다고
   사전 등록에 **적고** 시작한다)
