# 결과 — 마무리 길이가 **창 밖을 읽던 것**을 막았다 (미결 `ho` 43번 ㉳ 3회차)

2026.09.15 · 판정 기준은 [`PREREGISTRATION.md`](PREREGISTRATION.md) 에
**구현 전에** 굳혔다(`50c5d7f`). 재현:

```bash
cd agent
uv run python eval/pending43_leak_fix/measure_leak_fix.py --check        # 기준 B
uv run python eval/pending43_dribble_metrics/measure_window_metrics.py   # 기준 C
uv run python eval/phaseA/eval_b6/selector_downstream.py                 # 기준 E (약 430초)
uv run python eval/pending43_leak_fix/compare_b6.py <전> <후> --label E-2
```

## 한 줄

**고쳤다. B·C·D 는 예측대로다** — 바뀐 것이 예측한 **정확히 둘**이고, 창 밖
읽기가 **0%** 가 됐고, **등급·점수는 290행에서 한 칸도 안 움직였다.**

🔴 **그런데 B-6 재실행이 이 회차보다 큰 것을 드러냈다 — 기준선이 재현되지
않는다.** 지금 코드는 저장소에 든 B-6 산출(2026-09-08 판)을 **다시 만들어 내지
못한다.** 원인은 **환경도 비결정성도 아니다**(둘 다 이 회차가 배제했다).
5~6절이 그 이야기이고, **그쪽이 이 회차의 가장 중요한 결과**다.

## 판정

| 기준 | 내용 | 결과 | |
|---|---|---|---|
| **A** | 변경 범위 — `extract_features` 안 1줄(+주석) | `post = ankle_speed[t:ft_end]` **1줄** | ✅ |
| **B** | 🔴 값까지 박은 예측 — 78산출 중 **정확히 둘** | **2개, 값도 예측대로** | ✅ |
| **C** | 🔴 짝 기준 — 누수가 사라지고, **값을 자르는 방식**으로 | 창 밖 **0%** · 짝 8건이 달라짐 | ✅ |
| **D** | 🔴 2회차 결론의 시험 — 등급·점수 불변 | 불일치 **0** (290행) | ✅ |
| **E-1** | run#1 이 기존 CSV 와 305행 1:1 · 임팩트 무관 6열 일치 | 🔴 **290행 · 6열 중 3열 불일치** | 🔴 |
| **E-2** | run#2 가 run#1 과 마무리 길이 두 열 빼고 일치 | **그 밖 불일치 0열** | ✅ |
| **F** | 회귀 검사 추가 · `pytest -q` | **415 통과** (사전 등록은 414로 적었다 — 4절) | 🔶 |

## 1. B — 예측한 둘, 값까지 맞았다

| 산출 | 전 | 후 | |
|---|---:|---:|---|
| `3R1kvNrGJK0 \| arm \| extension_peak` | 109 | **104** | ✅ 예측대로 |
| `goeDWOVL95g \| arm \| extension_peak` | 56 | **47** | ✅ 예측대로 |

**다른 지표 불일치 0 · 임팩트·구간 불일치 0.** 세 번째가 바뀌면 불합격이라고
적어 뒀고, 안 바뀌었다.

## 2. C — 누수가 사라졌다 (2회차와 같은 계기, 같은 표본)

| `L` | 짝 | 누수지표가 창 없음과 동일 | 창 밖을 읽음 |
|---:|---:|---|---|
| 10 | 23 | 23/23 → **15/23** | 35% → **0%** |
| 15 | 29 | 29/29 → **20/29** | 31% → **0%** |
| 30 | 34 | 34/34 → **28/34** | 15% → **0%** |
| 60 | 39 | 39/39 → **33/39** | 15% → **0%** |

**둘이 함께 움직였다** — 창 밖 읽기가 0 이 되고(C-1), 같은 임팩트인데도 값이
갈라지기 시작했다(C-2). 한쪽만 섰으면 값을 통째로 망가뜨린 것이었다.

잘린 크기의 보기(창 / 창 없음):

| 산출 · 창 | 전 | 후 |
|---|---:|---:|
| `ZS-wgeg2qkI \| leg` (210, 220) | 86 | **6** |
| `Zp9aDp2YTBw \| leg` (40, 50) | 47 | **2** |
| `3R1kvNrGJK0 \| arm` (180, 195) | 104 | **4** |

**10프레임짜리 창에서 「마무리 86프레임」이 나오고 있었다.**

🔴 **함정 하나 — 2회차 스크립트는 다시 돌리면 자기 `window_metrics_raw.json` 을
덮어쓴다.** 이 회차에서 실제로 덮어썼고 **되돌렸다**(그 파일은 그 회차의
기록이다). 다시 돌릴 사람은 출력만 다른 이름으로 받을 것 —
이 회차의 재측정본은 `rerun_2회차_after.out` 이다.

🔴 **2회차 스크립트를 다시 돌리면 그 회차의 기준 A 가 2건 불일치로 뜬다** —
사전 등록에 적어 둔 그대로이고, 그것이 이 회차의 기준 B 다.
`pending45_multi_event/before.json` 은 **그 회차의 기록이라 갱신하지 않았다.**

🔴 **그리고 2회차 2·3절은 한 숫자도 안 바뀌었다** — 창이 지표를 **축소**시켜
등급이 44% 내려가던 것은 **그대로 남는다.** 이 회차는 **누수만** 고쳤다.

## 3. D — 2회차의 「누수는 근거 문장으로 나간다」가 맞았다

B-6 290행에서 `grade` · `grade_changed` · `score` **불일치 0.** 즉 이 값이
바뀌어도 **점수는 한 칸도 안 움직인다.** 2회차가 코드를 읽고 한 말
(`bands.metric` 은 `swing_hip_flexion_after_impact_deg` 이고 누수지표는
`measured_by` 에만 있다)이 **실물 290행에서 확인됐다.**

🔴 **뒤집어 말하면 이 결함은 점수로는 절대 안 드러난다.** 근거 문장에만
틀리게 실린다 — 그래서 2회차가 창을 걸어 재기 전까지 아무도 못 봤다.

## 4. F — 🔴 사전 등록과 어긋난 자리 (줄이지 않았다)

사전 등록에 **「검사 하나 추가 · 413 → 414」**라고 적었는데 **둘을 넣어 415**다.
창 **없는** 경우(유효 구간 밖을 읽음)와 창 **있는** 경우(창 밖을 읽음)는 같은
검사로 둘 다 못 잡는다.

| 검사 | 막는 것 |
|---|---|
| `test_the_deceleration_search_stays_inside_the_follow_through` | 창 없이도 **유효 구간 밖**(미검출)을 읽는 것 |
| `test_a_window_bounds_the_follow_through_length` | 구간을 줘도 **창 밖**을 읽는 것 |

**둘 다 헛돌지 않는지 확인했다** — 옛 규칙을 평가 쪽에 복제해 같은 입력에
대 보니 7 대 6 · 45 대 24 로 갈린다. **검사가 실제로 잡는다.**

🔴 **숫자를 맞추려고 검사를 지우지 않았다.** 어긋난 것을 그대로 적는다.

## 5. 🔴 E-1 — **기준선이 재현되지 않는다**

run#1(고치기 **전** 코드)을 저장소의 B-6 산출과 대 봤다.

| | |
|---|---|
| 행 수 | **305 → 290.** 3클립 × 5 selector = 15행이 빠졌다 |
| 빠진 클립 | `baseball_pitch_trim.mp4` · `bball_shot.mp4` · `bball_layup_trim.mp4` |
| 이유 | **설명된다** — 2026-09-11 축구 단일 종목 전환(미결 39번)으로 야구·농구 루브릭을 지웠고, Track 2 는 루브릭이 없으면 건너뛴다 |
| 겹친 290행 | 🔴 **85행이 다르다. 전부 Track 2** (17클립 × 5) |
| Track 1 | ✅ **195행 전부 일치** |
| 어긋난 열 | 17개. `impact_frame`(10행) · `grade`(10행, `B`→`D` 포함) · `swing_knee_angle_at_impact`(30행, `151.0`→`47.1` 같은 큰 차이) · 🔴 **임팩트 무관이어야 할 `detected_frames`·`usable_ratio_arm`·`usable_ratio_leg`** |

🔴 **RERUN.md 가 그 여섯 열에 대해 「여기서 어긋나면 임팩트 변경이 아니라
환경이 변한 것」이라고 적어 두었다.** 그래서 사전 등록의 멈춤 규칙이 발동한다.

### 무엇이 원인이 **아닌지**는 이 회차가 좁혔다

| 배제된 것 | 근거 |
|---|---|
| **패키지·GPU 환경** | torch 2.8.0+cu126 · cuDNN 91002 · transformers 5.15.1 · opencv 5.0.0 · numpy 2.5.2 · TF32 설정까지 **RERUN.md 의 2026-09-01 표와 같다** |
| **모델 가중치 (N-1)** | HF 캐시에 스냅숏이 **각 하나뿐**이고 날짜가 **8월 25일**이다. 고정한 해시가 그 하나다 — 갈아탈 것이 없었다 |
| **비결정성 (N-2·N-3)** | 🔴 **E-2 가 답했다** — 같은 코드로 두 번 돌린 290행이 **마무리 길이 두 열 말고 전부 비트 동일**이다. 검출·포즈·selector 가 이 환경에서 **재현된다** |
| **읽은 프레임 수 (N-4·메모리 가드 변경)** | `frames` 열이 **한 행도 안 바뀌었다** |
| **루브릭 정의** | 2026-09-08 이후 축구 루브릭 변경은 **항목 이름뿐**이다(`addf78d`) |
| **품질 게이트** | `check_quality` 는 `gate_ratio` 로 **추출만** 됐고 규칙이 같다 |

### ~~그래서 남은 것은 **코드 변경**이다~~ 🔴 **틀렸다 (2026.09.15, 47번 1회차가 같은 날 뒤집었다)**

> **코드가 아니다.** 2026-09-02 · 09-08 · 오늘 세 시점 코드의 Track 2 재실행이
> **서로 불일치 0** 이다. 그리고 기준선의 출처도 아래와 다르다 — **Track 2 CSV
> 는 09-08 갱신에서 한 바이트도 안 바뀌었고 실제로는 09-02 산출**이다(md5).
> 정본은 `../pending47_baseline_audit/RESULTS.md`. **아래 문단은 그때의 추론
> 기록으로 남긴다 — 지우면 같은 길을 또 간다.**

기존 CSV 는 **2026-08-28 이 아니라 2026-09-08 산출**이다(`426de4d`. RERUN.md 의
「2026-08-28」은 낡았다 — 09-02 와 09-08 에 두 번 갱신됐다). 그 뒤 **Track 2 가
쓰는 경로**를 건드린 커밋이 여섯이다:

| 커밋 | 무엇 |
|---|---|
| `056f1ff` (09-10) | 미결 37번 — 촬영 방향 의존 드러내기 |
| `c5ad695` (09-10) | 미결 `jin` 27번 — 봉투 계약 |
| `ef59faa` (09-11) | 가중치 고정 (로딩 경로가 바뀌었다) |
| `df7ef61` (09-11) | ㉮ 종목 게이트 (검출에서 도구 클래스를 함께 본다) |
| `51d7c75` (09-11) | ㉯ 사람 목록 (`detect_candidates`) |
| `d57600f` (09-14) | 45번 4회차 — `window` |

🔴 **Track 1(검출을 캐시에서 읽음)은 멀쩡하고 Track 2(검출을 즉석 실행)만
갈렸다.** 그래서 **검출 경로**가 가장 유력하다.

**결정적 검사(다음 회차)**: 위 커밋들에서 **Track 2 만** 한 번씩 돌린다
(116초/회). 어디서 갈라지는지 바로 나온다. 🔴 **B-2\~B-5 의 결론이 함께
흔들릴 수 있으므로**(RERUN.md 「불일치가 나오면 캐시가 아니라 기준선
감사다」) 별도 항목으로 올린다 — 미결 `ho` 47번.

## 6. 🔴 멈춤 규칙에 대해 — 내가 사전 등록을 잘못 썼다

사전 등록에 *"E-1 이 어긋나면 B·C·D 판정은 거기서 멈춘다"* 고 적었다. **E-1 은
어긋났다.** 규칙대로 **E-1 은 불합격으로 적는다.**

🔴 **그런데 그 규칙은 「E-1 이 격리를 보장한다」는 전제로 쓴 것이고, 그 전제가
틀렸다.** 격리는 **E-2 가 혼자 세운다** — 같은 코드·같은 환경에서 한 줄만
다른 두 실행의 대조다. B 와 C 는 아예 B-6 을 안 쓴다(캐시 키포인트를 읽는다).

**그래서 이렇게 적는다**: B·C·D 는 **E-1 과 무관하게 성립한다.** 이것은 결과를
보고 기준을 고친 것이 **아니라**, 기준들 사이의 **의존 관계를 처음에 잘못
적었다는 것을 드러내는 것**이다. 🔴 **다음 사전 등록부터는 멈춤 규칙을 걸기
전에 「어느 판정이 어느 기준에 실제로 의존하는가」를 먼저 적는다.**

## 7. 이 회차가 남긴 것

| | |
|---|---|
| 코드 | `features.py` **1줄** + 주석 · 회귀 검사 **2개** |
| B-6 산출 | 저장소의 두 CSV 를 **run#2 로 갱신**했다. 2026-09-08 판은 git 이력에 있다(`426de4d`) |
| 소요 | run#1 **435초** · run#2 **428초** (RERUN.md 의 244초는 낡았다 — 갱신함) |
| 재현성 증거 | 🔴 **이 환경에서 B-6 은 실행 간 재현된다** (E-2, 290행). RERUN.md 의 N-2·N-3 위험에 **처음으로 실측이 붙었다** |
| 남은 것 | 🔴 **기준선 불일치**(미결 47번) · 🔴 **밴드 문제**(2회차 4절, 제품·검수 판단) |

🔴 **「드리블 채점이 고쳐졌다」고 적지 않는다.** 고친 것은 **창 밖을 읽던 것**
하나다.
