# 사전 등록 — **09-08 과 09-15 사이에 이 기계에서 무엇이 바뀌었나** (미결 `ho` 47번 4회차)

2026.09.16 · 🔴 **아무 증거도 보기 전에 굳힌다.** 이 파일을 커밋한 뒤에 찾기
시작한다.

🔴 **조사 회차다** — `src/`·`rubrics/`·보존 자산을 고치지 않는다. 계기
(`run_meta.json`)만 손대고, 그것이 산출을 안 건드린다는 것을 기준 E 로 확인한다.

## 왜 이 질문인가

3회차까지 배제된 것: 입력 파일 · 코드 · 의존성 목록(`uv.lock`)·드라이버·가중치 ·
실행 간 비결정성 · 여유 VRAM · **검출·selector**. 창은 **09-08 ~ 09-15** 로
좁혀졌고(09-08 에는 독립 기록으로 재현이 확인됐다), 움직인 자리는 **포즈
이후**다.

🔴 **남은 것은 「기록되지 않은 무엇」이고, 그것을 기록이 아니라 흔적으로 찾는다.**
파일시스템의 mtime, 로그에 섞여 든 경로, 커널 문자열은 아무도 남기려고 남긴
것이 아니라서 **그날의 의도와 무관하게 남아 있다.**

## 세 갈래 — 무엇이 맞으면 무엇이 닫히는가

| | 가설 | 맞으면 왜 관찰과 맞는가 |
|---|---|---|
| **H1** | **09-08 실행이 이 기계가 아니었다** (EC2 T4 등 다른 GPU) | 커널 선택이 달라 포즈 회귀값이 미세하게 흔들린다. 검출 박스는 튼튼해 안 움직인다 — 3회차가 본 모양 그대로다 |
| **H2** | **같은 기계인데 파이썬 환경이 바뀌었다** (`.venv` 재설치 · torch/opencv 재설치 · 드라이버·WSL 커널 갱신) | 🔴 **`uv.lock` 무변경은 「설치된 바이트가 그대로」를 뜻하지 않는다** — 같은 버전으로 다시 깔아도 빌드가 다를 수 있고, 커널 라이브러리(cuDNN/cuBLAS)는 wheel 안에 들어 있다 |
| **H3** | **`.avi` 디코딩** (3회차가 남긴 가설) | 픽셀이 미세하게 달라지면 검출은 그대로인데 포즈가 흔들린다 |

## 판정 기준 — 결과를 보고 바꾸지 않는다

| | 기준 | 합격/판정선 |
|---|---|---|
| **A** | 🔴 **계기 검사 — mtime 이 「설치 시각」을 뜻하는가.** 대조군으로 (ㄱ) 확실히 옛날 파일 (ㄴ) 방금 내가 만든 파일의 mtime 을 함께 본다 | 대조군이 안 서면 **mtime 증거를 판정에 쓰지 않고 「옆에 붙임」으로 적는다.** 8·10회차가 계기 때문에 두 번 망했다 |
| **B** | **H1** — 09-08 실행 위치의 **직접 증거**(로그·산출물에 남은 절대경로·호스트명·장치명) | 증거가 있으면 판정한다. **없으면 「판별불가」라고 적는다** — 「아마 이 기계였다」로 쓰지 않는다 |
| **C** | **H2** — 핵심 패키지(torch · cv2 · transformers)와 `.venv` 자체의 설치 흔적이 **09-08~09-15 창 안에 있는가** | 하나라도 창 안이면 H2 **살아남음**(원인 후보로 남긴다) · 전부 창 밖이면 H2 **닫힘** |
| **D** | **H3** — 같은 `.avi` 를 **두 번** 디코딩한 프레임 md5 | **같아야 한다.** 다르면 그 자체가 답이고 (그 경우 H3 즉시 승격) |
| **E** | **계기 추가가 산출을 안 건드린다** — 디코딩 지문을 넣은 뒤 Track 2 CSV 를 오늘 기존 산출과 대조 | **불일치 0** (40열 × 95행). 🔴 어기면 지문 추가를 되돌린다 |
| **F** | `src/`·`rubrics/`·보존 자산 무변경 | **0줄** |

## 🔴 하지 말 것

- **기준선 CSV 를 덮어쓰지 않는다** — 그것이 (나)이고, **원인을 모른 채 덮으면
  같은 일이 또 나도 못 알아본다.** 3회차까지의 순서 결정을 뒤집지 않는다
- `eval_b2/eval_b2.py` 의 selector 가중치를 건드리지 않는다 (B-2~B-5 전체 무효)
- **mtime 하나로 「원인 규명」이라고 적지 않는다.** 설치 시각이 창 안에 있다는
  것은 **후보가 살아 있다**는 뜻이지 그것이 값을 바꿨다는 증명이 아니다
- 🔴 **「원인을 모른다」를 피하려고 살아남은 가설 하나를 결론으로 승격하지
  않는다.** 3회차까지 세 번 다 좁히기만 했고 그것이 정직한 답이었다
- 디코딩 지문을 **`features` 나 판정 입력에 넣지 않는다** — 형제 블록(메타)이다.
  넣으면 B-6 재실행을 부른다

## 예상 (틀리면 그대로 적는다)

**H2 가 살아남고 H1·H3 가 닫힐 것**으로 본다.

🔴 **이 예상이 어떤 분포에서 나왔는지**: 근거는 「09-14~09-15 에 표본·봉투
작업을 하며 `uv sync` 가 돌았을 개연성」 **하나**이고, 실제 설치 이력을 본 적은
없다. 13회차에서 집계값 하나로 기전을 추정해 두 번 연속 틀렸으므로 **약한
예상으로 적는다.**

## 확인 (이 회차가 끝나면 돌아갈 자리)

```bash
cd agent
uv run python eval/pending47_baseline_audit/run_track2_at.py --help   # 기존 도구
git diff --stat -- src/ rubrics/                                      # 기준 F: 0줄
```
