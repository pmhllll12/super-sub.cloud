# 사전 등록 — **어느 커밋이 B-6 Track 2 를 갈랐나** (미결 `ho` 47번 1회차)

2026.09.15 · 🔴 **돌리기 전에 굳힌다.** 결과를 보고 이 문서를 고치지 않는다.

## 무엇을 묻는가

43번 ㉳ 3회차가 발견했다 — 저장소의 B-6 산출(**2026-09-08** 판, `426de4d`)이
지금 코드로 **재현되지 않는다.** 겹치는 290행 중 **85행이 다르고 전부
Track 2**다(Track 1 은 195행 전부 일치).

그 회차가 원인을 좁혀 뒀다: **환경·가중치·비결정성·읽은 프레임 수·루브릭
정의·품질 게이트가 전부 아니다.** 남은 것은 **2026-09-08 이후의 코드 변경**이고,
후보가 17커밋이다.

**이 회차는 그 갈림점을 특정한다.**

## 어떻게

`426de4d`(good, 그 CSV 를 낸 커밋) 와 `d57600f`(bad, 43번 ㉳ 3회차 **직전**)
사이를 **이분 탐색**한다.

🔴 **내 어제·오늘 변경(`99f12b9`)은 범위에서 뺀다** — 갈림은 그 앞에서 이미
있었다(run#1 이 고치기 **전** 코드였다).

| | |
|---|---|
| 계기 | 워크트리를 하나 파고 커밋을 옮겨 가며 **Track 2 만** 돌린다 (약 120초/회) |
| 비교 | 축구 19클립 × 5 selector = **95행**을, `426de4d` 판 CSV 의 같은 95행과 **전 열 대조** |
| good/bad | 불일치 **0행** = good · 하나라도 다르면 bad |
| 🔴 뺀 것 | 야구·농구 3클립(15행)은 **39번 종목 정리로 사라진 것이 이미 설명된다** — 비교에서 뺀다 |

`agent/data/` 는 `.gitignore` 라 워크트리에 없다 — **심링크로 붙인다.**
🔴 **치울 때 `git worktree remove` 로만 치운다**(심링크를 지우다 원본을 날리지
않게).

## 판정 기준

| | 내용 | 합격선 |
|---|---|---|
| **A** | 🔴 계기 검사 — Track2 전용 드라이버가 **HEAD 에서** 오늘 run#2 의 Track 2 95행과 **비트 동일** | 불일치 **0**. 어긋나면 **회차 무효** |
| **B** | 갈림점을 **하나 특정**한다 | 커밋 해시 + 몇 번 돌렸는지 보고 |
| **C** | 🔴 **기전 설명** — 그 커밋의 diff 로 **왜 Track 2 만 갈리고 Track 1 은 안 갈리는지** 말할 수 있는가 | 설명 못 하면 **「특정했다」고 적지 않는다** |
| **D** | 그 변경이 ⑴ **의도한 개선**인가 ⑵ **모르고 낸 회귀**인가 | 근거와 함께 적는다 |
| **E** | `src/`·`rubrics/`·보존 자산 무변경 | `git status --porcelain` **0줄** |
| **F** | `uv run pytest -q` | **415 통과** |

🔴 **갈림이 하나가 아닐 수 있다.** 이분 탐색은 **첫** 전이만 찾는다. 찾은 뒤
남은 구간에 또 전이가 있는지 **한 번 더 확인**하고, 있으면 **몇 개인지**
보고한다(전부 특정하는 것은 이 회차의 합격 조건이 아니다).

## 🔴 예측 (짝) — 적어 두고 대 본다

- **P-1 (구조)** 갈림은 **입력 프레임(`read_frames`) 쪽**이지 `features`·
  `scoring` 쪽이 **아니다.** 근거: 달라진 열에 **`detected_frames` 와
  `usable_ratio_*`** 가 들어 있는데, 그 둘은 **포즈 이전**(어떤 픽셀을
  넣었는가)에서 갈린다. 지표 계산을 바꾸는 커밋으로는 그 열이 안 움직인다.
- **P-2 (지점)** 구체적으로 **`3b05436`**(메모리 가드를 장수에서 바이트로,
  `read_frames` 의 `max_frames` 기본값이 300 → 해상도 계산).

🔴 **P-1 이 틀리면 내 인과 모형이 틀린 것이고 그렇게 적는다.** P-2 만 틀리는
것은 예측 실패로 적되, P-1 이 서면 회차는 선다.

🔴 **P-1 에는 이미 걸리는 것이 하나 있다** — `read_frames` 는 **Track 1 도**
쓴다. 그런데 Track 1 은 195행 전부 일치다. **그래서 P-1 이 맞으려면 「`.avi`
또는 그 해상도에서만 달라진다」가 함께 성립해야 하고, 기준 C 가 그것을
설명하라고 요구한다.** 설명이 안 되면 P-1 은 맞아도 **반쪽**이다.

## 🔴 하지 말 것

- **고치지 않는다.** 이 회차는 **조사**다. 회귀로 판정되면 **다음 회차에
  사전 등록**하고 고친다.
- **CSV 를 다시 덮어써 「맞췄다」고 하지 않는다** — 값이 왜 달라졌는지가
  답이지 최신화가 답이 아니다.
- **`eval_b2/eval_b2.py`(보존 자산)를 건드리지 않는다** — selector 가중치를
  고치면 B-2\~B-5 가 통째로 무효다.
- **결과를 보고 예측·기준을 바꾸지 않는다.**
- 🔴 **「B-2\~B-5 도 무효다」를 이 회차로 단정하지 않는다.** 그 판단은 갈림의
  기전을 알고 나서 할 일이고, 여기서는 **무엇이 달라지는가**까지다.
