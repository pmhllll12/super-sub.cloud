# 결과 — **입력은 아니다. 창이 09-08\~09-15 로 옮겨졌다** (미결 `ho` 47번 2회차 · (가))

2026.09.15 · 판정 규칙은 [`PREREGISTRATION_2.md`](PREREGISTRATION_2.md) 에
**찾기 전에** 고정했다(`2052a7c`).

🔴 **조사 회차 — `src/`·`rubrics/` 무변경** · **GPU 안 씀**(md5 와 git 만 봤다).

## 한 줄

찾던 것(**09-02 에 쓰던 클립**)은 **없었다.** 그런데 **다른 기록이 답을
줬다** — 2026-09-08 의 미결 21번 회차가 **B-6 전 구간을 다시 돌려 Track 2
완전 일치**를 확인해 두었다. **그날은 재현됐다.**

**그래서 「언제 깨졌나」의 창이 09-02\~09-04 가 아니라 09-08\~09-15 다.**
그 창에서는 **입력이 md5 로 고정**돼 있으므로(09-04부터 오늘까지 동일),
**⑴ 입력 가설은 사실상 배제된다.**

## A — 사본: 하나 있고, **19/19 md5 일치**

| | |
|---|---|
| 찾은 것 | `/mnt/d/sports_dataset/soccer/clips/batch_0000/*.avi` **19개** — 수집 파이프라인이 **2026-09-04** 에 바이트 복사한 것(`LocalFolder.fetch` 가 `write_bytes` 로 뜬다) |
| 대조 | 오늘 `data/goldenset/soccerkicks_video/*.avi` 와 **md5 19/19 일치 · 불일치 0** |
| 뜻 | **2026-09-04 → 오늘, 입력은 한 바이트도 안 바뀌었다** |
| 더 있나 | 🔴 **없다.** `/mnt/d` 와 `/home/ho` 를 **파일 크기로 전수 검색**해도 나오는 것은 이 둘뿐이다 |

## B — 재취득: **길이 없다**

`data/goldenset/soccerkicks/General annotations/video_source.csv` 가 이 클립들의
출처를 적어 두었다 — **유튜브 영상을 온라인 커터로 잘라 만든 것**이다
(`Link`·`Time`·`Site video trim` 컬럼). 즉 **제3자가 만든 트림본**이고,
같은 바이트를 다시 만들 길은 없다. 우리가 받은 배포본을 다시 받는 경로도
문서에 없다.

🔴 **`/mnt/d/sports-pose/soccer/raw/3D-Shot-Posture-Dataset` 는 다른 것이다** —
3DSP 는 **이미지 시퀀스**(`3dsp.zip`, 2026-09-10 에 받음)이고 이 `.avi` 가 아니다.

## C — 09-02\~09-04 창의 기록: **없다**

그 창에 추가된 `agent/eval`·`agent/data` 산출물 중 축구 클립 이름을 담은 것이
**하나도 없다**(전수 검색). 09-09 의 `pending7_realfps/run.out` 이 클립별 원본
fps·프레임 수를 들고 있지만 **창 밖**이다.

## 🔴 그런데 — **09-08 에는 재현됐다**

`eval/pending21_hip_rotation/` (미결 21번, 2026-09-08):

| | |
|---|---|
| 그 회차가 한 일 | **B-6 전 구간 재실행** (Track1 330초 / Track2 142초, RTX 3050 cuda) |
| Track 2 판정 | **키 집합·`impact_frame`·지표 8개+`delta_*`·`grade`·`score`·`features_ok` 전부 일치, `hip_rotation_range_deg` 변화 0행** |
| `before_rubric_clips.csv` | md5 **`7e41a201…`** — 커밋된 CSV 와 **같은 파일** |

🔴 **그 회차의 기준 A 가 「selector·포즈 의존 7컬럼이 어긋났다면 내 변경이
아니라 환경이 변한 것」이라고 적어 두었고, 그것이 완전 일치였다.**
**2026-09-08 에 이 기계는 09-02 의 Track 2 를 그대로 다시 만들어 냈다.**

그리고 오늘(09-15) 같은 커밋(`426de4d`)으로 돌리면 **85/95행이 다르다**
(1회차). 두 사실을 겹치면:

| 창 | 무엇이 그 안에 있나 |
|---|---|
| ~~09-02 \~ 09-04~~ | 닫혔다 — 그 뒤 09-08 에 재현됐으므로 |
| **09-08 \~ 09-15** | 🔴 **여기서 깨졌다.** 그런데 이 창에서 **입력은 md5 로 동일**하고(09-04부터), `uv.lock` 무변경, 드라이버 560.94 동일, 코드는 세 시점이 서로 같은 값을 낸다(1회차) |

## 🔴 사전 등록 판정 규칙의 결함 — 결론은 같고 **경로가 다르다**

규칙표에 *「A 또는 B 로 md5 가 일치 → ⑴ 배제」*라고 적었는데, **사본의 시점을
안 따졌다.** 찾은 사본은 **09-04** 이고 문제의 실행은 **09-02** 라, 그 규칙을
문자 그대로 적용하면 **틀린 말**이 된다.

✅ **같은 문서의 「예상」에는 그 한계를 적어 뒀다** — *"사본이 오늘 것과 같으면
「적어도 두 자리가 일치한다」까지이고, 그것으로 ⑴ 을 배제하지는 못한다."*
**예상이 규칙보다 정확했다.**

**⑴ 을 실제로 배제한 것은 사본 대조가 아니라 09-08 의 재현 기록**이다.
그대로 적는다. 🔴 **다음 사전 등록부터 「증거에 시점을 붙인다」** — 지난번
멈춤 규칙과 같은 종류의 실수다(무엇이 무엇을 보장하는지 안 쓰고 규칙만 썼다).

## 그래서 남은 것 — **실행 시점의 기계 상태**

코드·입력·의존성·드라이버·가중치가 전부 같은데 **09-08 에는 되고 09-15 에는
안 된다.** 남은 자리는 **그때 그 실행이 놓였던 상태**다. RERUN.md 가 경고해 둔
바로 그 둘이 후보다:

| 후보 | 왜 그럴듯한가 | 왜 아직 가설인가 |
|---|---|---|
| **다른 프로세스의 GPU 점유** (N-2·N-3 계열) | 이 기계는 vLLM 도 띄운다. 여유 VRAM 이 다르면 커널·워크스페이스 선택이 달라질 수 있고, **검출 문턱 0.5 의 절벽**이 그 미세차를 박스 하나로 증폭한다 | `cudnn.benchmark=False` 라 자동탐색은 꺼져 있다. **재 봐야 안다** |
| 기록되지 않은 무엇 | — | 🔴 **09-08 실행의 메타가 없다.** (다) 가 그래서 나왔다 |

✅ **오늘 `run_meta.json` 에 칸을 하나 더 넣었다** — `gpu_free_bytes_at_start`·
`gpu_total_bytes`. 🔴 **이 회차가 그 칸이 없다는 것을 알려 줬다**(과거는 여전히
기록이 없다. 앞으로만이다).

## 다음 (고르지 않았다)

| | |
|---|---|
| **(가-2) 가설 검사** | VRAM 을 일부러 점유한 채 **Track 2 만** 돌려(116초) 값이 움직이는지 본다. **움직이면 원인 규명**이고, 안 움직이면 이 후보가 닫힌다 |
| **(나) 기준선 재선언** | 1회차가 적어 둔 그대로. 🔴 **(가-2) 를 먼저 한다** — 원인을 모른 채 덮으면 같은 일이 또 나도 못 알아본다 |

🔴 **「입력이 아니다」까지는 이 회차가 세웠다.** 그 이상은 아직 가설이다.
