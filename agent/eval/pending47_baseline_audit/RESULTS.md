# 결과 — **갈림점은 없었다. 코드가 원인이 아니다** (미결 `ho` 47번 1회차)

2026.09.15 · 판정 기준은 [`PREREGISTRATION.md`](PREREGISTRATION.md) 에
**돌리기 전에** 굳혔다(`259007c`). 재현:

```bash
cd agent
# 워크트리에 커밋을 놓고 Track 2 만 돌린다 (약 120초/회)
uv run python eval/pending47_baseline_audit/run_track2_at.py --worktree <wt> --out <csv>
uv run python eval/pending47_baseline_audit/verdict.py <csv>
```

🔴 **조사 회차다 — `src/`·`rubrics/`·보존 자산 무변경**(기준 E, 0줄) ·
`pytest -q` **415 통과**(기준 F).

## 한 줄

**이분 탐색을 시작하기도 전에 끝났다.** 양 끝점을 재 보니 **`good` 쪽이
없었다** — 그 CSV 를 낸 커밋으로 돌려도 안 맞는다. 그리고 **2026-09-02 코드 ·
09-08 코드 · 오늘 코드 세 재실행이 서로 불일치 0** 이다.

🔴 **그래서 어제 제가 「남은 것은 코드 변경이다」라고 적은 것은 틀렸습니다.
코드는 원인이 아닙니다.** 사전 등록한 예측 **P-1·P-2 둘 다 틀렸다.**

## 판정

| 기준 | 내용 | 결과 | |
|---|---|---|---|
| **A** | 🔴 계기 검사 — Track2 전용 드라이버가 HEAD 에서 오늘 run#2 와 비트 동일 | **95행 × 40열 불일치 0** | ✅ |
| **B** | 갈림점을 하나 특정 | 🔴 **없다** (아래 2절) | — |
| **C** | 기전 설명 | 갈림 커밋이 없다. 대신 **왜 Track 2 만인가**에 답이 생겼다 (4절) | ✅ |
| **D** | 의도한 개선인가 회귀인가 | **둘 다 아니다** — 코드 변경이 아니다 | ✅ |
| **E** | `src/`·`rubrics/`·보존 자산 | **0줄** | ✅ |
| **F** | `pytest -q` | **415** | ✅ |
| **P-1** | 🔴 예측 — 갈림은 `read_frames` 쪽 | **틀렸다** | 🔴 |
| **P-2** | 🔴 예측 — `3b05436` 에서 갈린다 | **틀렸다** | 🔴 |

## 1. 🔴 먼저 — 기준선의 출처가 내가 적은 것과 달랐다

43번 ㉳ 3회차에서 「기존 CSV 는 `426de4d`(2026-09-08) 산출」이라고 적었다.
**Track 2 는 아니다.** md5 가 답한다:

| 커밋 | Track 1 CSV | Track 2 CSV |
|---|---|---|
| `4626870` (2026-09-02) | `43554b49…` | **`7e41a201…`** |
| `426de4d` (2026-09-08) | `4ebe2a75…` (바뀜) | **`7e41a201…` (그대로)** |

**09-08 갱신에서 Track 2 파일은 한 바이트도 안 바뀌었다.** 그래서 비교 대상은
**09-02 산출**이고, 사전 등록이 잡은 탐색 구간(`426de4d..d57600f`)은 **애초에
범위를 잘못 잡은 것**이었다.

🔴 **탐색 규칙(불일치 0 = good)은 그대로 두고 구간만 `4626870..426de4d` 로
옮겼다.** 기준을 결과에 맞춰 고친 것이 아니라 **어느 커밋이 그 산출을
냈는지에 대한 사실을 고친 것**이다.

## 2. 그런데 **옮긴 구간의 good 끝도 bad** 였다

| 실행한 커밋 | 날짜 | 기준선(09-02 판)과 | |
|---|---|---|---|
| `4626870` | 09-02 · **그 CSV 를 낸 커밋 자체** | 🔴 **85/95행 다름 · 18열** | bad |
| `426de4d` | 09-08 | 85/95행 · 18열 | bad |
| `HEAD` | 09-15 | 85/95행 · 18열 | bad |

**세 프로필이 행 수·열 수·열별 건수·최대차까지 전부 같다.**

## 3. 🔴 결정적 — 재실행끼리는 **완전히 같다**

| 대조 | 공통 열 | 불일치 |
|---|---:|---:|
| `4626870` vs `426de4d` | 40 | **0** |
| `426de4d` vs `HEAD` | 40 | **0** |

**2026-09-02 부터 오늘까지 어떤 커밋도 Track 2 의 값을 바꾸지 않았다.**
(덤: 오늘 고친 마무리 길이도 **Track 2 축구 95행에는 영향이 없다** — 바뀐 두
산출이 `arm` 설정이라 Track 1 쪽이었다.)

그러므로 차이는 **버전 관리 밖**에서 오고, **재현 가능한(결정적) 차이**다.

## 4. 그래서 무엇이 남는가

3회차가 이미 배제한 것(환경·가중치·**실행 간 비결정성**·프레임 수·루브릭·
품질 게이트)에 이 회차가 넷을 더했다:

| 추가로 배제 | 근거 |
|---|---|
| **코드 변경 전부** | 3절 — 세 시점 코드가 서로 불일치 0 |
| **의존성 갱신** | `uv.lock`·`pyproject.toml` 이 2026-09-02 이후 **무변경** |
| **GPU 드라이버** | **560.94** — RERUN.md 기록과 같다 |
| **N-2(OOM 배치 폴백)** | 🔴 **Track 2 에서는 구조적으로 불가능하다** — `_pose_batch` 는 한 프레임의 박스 수(1~3개)만 넣고 `MAX_BATCH` 는 24 다. 쪼갤 일이 없어 배치 크기가 애초에 안 변한다 |

**남은 후보는 둘뿐이고, 둘 다 오늘은 확인할 수 없다:**

| | |
|---|---|
| ⑴ **입력 클립이 그때와 다르다** | Track 2 의 입력 `agent/data/` 는 **`.gitignore`** 이고 **체크섬이 어디에도 기록돼 있지 않다.** 파일 mtime 은 2021년(원본 보존)이라 아무것도 말해 주지 않는다 |
| ⑵ **그 CSV 가 이 기계·이 상태의 산출이 아니다** | 작업 트리가 커밋과 달랐거나, 다른 기계에서 돌렸을 수 있다. 실행 메타를 **파일로 안 남기기로** 되어 있어(스크립트 주석) 확인할 방법이 없다 |

🔴 **그리고 이것이 「왜 Track 2 만인가」에 답한다** — Track 1 의 입력은
`/mnt/d/…/clips` + **저장소에 든 `candidates_target30/`** 라 위치가 고정돼
있고, Track 2 의 입력만 **버전 관리 밖**이다. 기준 C 가 요구한 설명이 이것이다.

## 5. 이 회차가 남긴 것 — **다음 감사가 1초에 답하도록**

`input_fingerprints.csv` — 평가 입력 **61개**(Track 2 22 · Track 1 39)의
**md5 와 크기**. 🔴 **이게 없어서 오늘 ⑴ 을 못 갈랐다.** 다음에 같은 일이
나면 이 파일과 대 보면 끝난다.

| | |
|---|---|
| Track 2 | `data/*.mp4` 3 · `data/goldenset/soccerkicks_video/*.avi` 19 |
| Track 1 | `<외부>/clips/*.mp4` 39 |

🔴 **이 지문은 「그때 쓰던 파일」이 아니라 「오늘 쓰는 파일」이다.** 과거를
복원해 주지는 않는다 — **오늘부터의 기준선**이다.

## 6. 그래서 B-2\~B-5 는 무효인가

**이 회차로 단정하지 않는다**(사전 등록의 「하지 말 것」). 다만 판단에 필요한
것이 하나 늘었다:

- 🔴 **Track 1 은 195행 전부 일치한다**(3회차). B-2\~B-5 의 selector 결론은
  **Track 1 계열 자산**(`candidates_target30/`·`eval_b2/pose_quality.csv`)
  위에 서 있고, 그쪽은 **저장소 안에 있고 재현된다.**
- 흔들리는 것은 **Track 2(22클립, 그중 축구 19)** 이고, 이쪽은 **B-6 의
  「루브릭이 있는 클립에서의 downstream 전이」** 절에 쓰인다.

**그래서 다음 회차의 질문은 「무효인가」가 아니라 「Track 2 결론 중 무엇이 이
85행에 기대고 있는가」다.**

## 7. 남은 길 (고르지 않았다)

| | |
|---|---|
| **(가) 입력을 갈라 본다** | 09-02 실행이 쓴 클립을 복원할 길이 있는지 — 없으면 ⑴ 은 **영구 미결**이고, 그러면 **오늘 지문을 기준선으로 재선언**하는 것이 답이다 |
| **(나) 기준선을 다시 세운다** | Track 2 CSV 를 오늘 산출로 교체하고 **지문·환경·커밋을 함께 박아 둔다.** 🔴 값이 왜 달랐는지를 모른 채 덮는 것이라, 그 사실을 문서에 남기는 것이 조건이다 |
| **(다) 실행 메타를 파일로 남긴다** | 지금은 stdout 에만 찍고 「승인된 산출물이 아니라」 파일로 안 남긴다. 그 규칙이 오늘 ⑵ 를 못 가르게 만들었다 — **규칙을 바꿀 값어치가 있다** |

🔴 **(나)를 (가)보다 먼저 하지 않는다** — 덮으면 ⑴ 을 영영 못 가른다.
