# 결과 — 다인 화면에서 고른 사람이 공을 다루는 사람인가 (미결 `ho` 18번 12회차)

2026.09.14 · 사전 등록 [`PREREGISTRATION.md`](PREREGISTRATION.md)(커밋 `2a9373c`,
**측정 스크립트를 쓰기 전에** 굳혔다) · 🔴 **`src/`·`rubrics/` 0줄 변경**

재실행:
```bash
cd agent && uv run python eval/sample_gate/fetch_soccernet.py --n 40   # 최초 1회 (hf auth login)
cd agent && uv run python eval/pending18_broadcast/measure_broadcast.py
cd agent && uv run python eval/pending18_broadcast/posthoc_why_not_perspective.py
```

---

## 판정 — **판별불가.** `M`·`margin` 은 (나)인데 **기전이 설명 안 된다**

| | 기준 | 결과 | |
|---|---|---|---|
| **A** | 주 층에 단일후보 **0건** | **0건** | ✅ |
| **B** | 🔴 계기 — ±1px 섭동에 `argmin` flip **< 20%** | **0.0%** (0/198) | ✅ |
| **C** | `src/`·`rubrics/` 무변경 | **0줄** (11회차를 import) | ✅ |
| **D** | reach 보고 | 주 층 **198/1,924 (10.3%)** · 클립 **9/40** | — |
| **E** | `NEAR` 네 값에서 판정 구간 불변 | 전부 같은 쪽 | ✅ |
| **F** | `pytest -q` | **413 통과** | ✅ |
| **G** | 🔴 짝 기준 — 불일치 높이비 중앙 **≥ 2.0** | **1.19** | 🔴 |

| | |
|---|---|
| 주 지표 `M` | **17.7%** (35/198) · 클립 가중 중앙 **8.3%** |
| 클립별 분포 | 0 · 0 · 0 · 0 · 8 · 9 · 10 · 38 · **77** % |
| 불일치 `margin` | 중앙 **1.687** 키높이 (범위 0.258~11.362) — `MIN_MARGIN` 0.25 의 **6.7배** |

**`M` 17.7% ≤ 30% 이고 `margin` 도 통과다.** 사전 등록대로면 (나)의 두 조건이
다 섰다. **그런데 G 가 깨졌고, 사전 등록이 그 경우를 「판별불가로 내린다」고
미리 적어 두었다.** 그대로 한다.

### 🔴 민감도 — 문턱에 업혀 있지 않다

| `NEAR` | 0.15 | **0.25** | 0.35 | 0.50 |
|---|---:|---:|---:|---:|
| 주 층 | 62f | **198f** | 341f | 540f |
| `M` | 11.3% | **17.7%** | 23.5% | 22.4% |
| `margin` 중앙 | 1.24 | **1.69** | 1.69 | 1.79 |

네 값 모두 `M ≤ 30%` · `margin ≥ 0.25` 다. 🔴 **표의 「(나)」 표기는 `M`·`margin`
부분만 본 것**이고, **G 는 표본 전체에 하나뿐이라 어느 `NEAR` 에서도 최종 판정은
판별불가**다. 스크립트 출력이 그 구분을 안 보여 주므로 여기 적는다.

---

## 🔴 내 예측이 틀렸다 — **두 겹으로**

사전 등록에 **「(나)가 나올 것」**이라고 박았다. 판정은 **판별불가**다.
그런데 더 중요한 것은 **왜** 틀렸는지다.

> 사전 등록 G: *"기전은 **원근**이어야 한다 — 방송 구도에서 「가장 큰 사람」 =
> 「카메라에 가장 가까운 사람」이다."*

**아니었다.** 불일치 프레임에서 고른 사람의 박스 높이는 최근접 후보의
**1.19배**(p25 **1.04** · p75 1.53)다. **거의 같은 크기다.** 카메라에서 훨씬
가까운 사람을 고르고 있는 것이 아니다.

🔴 **이 이야기는 관문을 돌린 뒤 프레임 하나를 눈으로 보고 세운 것이다.** 사전
등록에 「나는 이미 한 프레임을 봤다」고 적어 두고 바를 1.2 → **2.0** 으로
올렸는데, **정직하게 말하면 그 조정은 결과를 바꾸지 않았다** — 측정값 **1.19**
는 **1.2 에서도 불통과**였다. 바를 올린 판단은 옳았지만 **그것이 결함을 막은
것은 아니다.** (막은 것은 G 를 **넣어 둔 것** 자체다.)

---

## ✅ 기전을 찾았다 — **원근이 아니라 「폭」이다**

`posthoc_why_not_perspective.py` (🔴 **판정에 안 쓴다** · 주 층 **198프레임**으로
본 회차와 **같은 층**이다 · 불일치 163프레임).

| | 고른 박스 | 최근접 후보 |
|---|---:|---:|
| 가로세로비 `w/h` | **0.61** (p75 0.76) | **0.50** (p75 0.58) |
| — | | |
| **면적비** (고른÷최근접) | **1.71** (p25 1.40 · p75 2.30) | |
| **높이비** | **1.19** (p25 1.04 · p75 1.52) | |

**면적비가 높이비보다 훨씬 크다.** `_largest_person_box` 는 **면적**으로 고르는데,
다인 화면에서 그 면적을 키우는 것은 **키가 아니라 폭**이다. 그리고 고른 박스가
그 프레임에서 **가장 넓은 박스인 비율이 64%**(105/163)다.

**「거의 동률이라 임의로 고른다」는 부차적이다** — 최대 면적의 10% 안에 든 후보가
2명 이상인 프레임은 **32%** 뿐이고 중앙값은 **1명**이다. 즉 argmax 는 대개
분명한데 **그 분명한 승자가 「폭이 넓은 것」**이다.

🔴 **폭이 넓은 이유가 무엇인지는 이 회차가 안 가른다.** 팔다리를 벌린 자세 ·
달리는 선수 · **겹친 사람 둘을 한 박스로 잡은 것** 중 어느 쪽인지 구분하지
않았다. 다음 회차의 질문이다.

---

## 🔴 이 회차가 **말하지 않는 것** (사전 등록 1절을 그대로 옮긴다)

| | |
|---|---|
| **제품 성능이 아니다** | 방송 중계이고 제품 입력은 휴대폰이다. 「선택 규칙이 **어떻게** 깨지는가」를 잰 것이지 **「제품에서 몇 %가 깨지는가」가 아니다** |
| **채점을 안 물었다** | 표본 전체의 사람 키 중앙이 **28px** 다. 이 회차는 **박스만** 썼다 |
| **「공 쪽이 맞다」가 아니다** | 공 근접은 라벨이 아니라 **물리적 개연성**이다. 말할 수 있는 최대치는 **「우리 규칙과 공이 서로 다른 사람을 가리킨다」** |
| 층이 **가까운 프레임에 몰린다** | 주 층에서 고른 박스 높이 중앙은 **86px**(24~168)로 표본 전체(28px)보다 훨씬 크다 — **공이 검출되려면 카메라가 가까워야** 해서다. 🔴 기준 B 가 0.0% 로 통과한 것도 그 덕이다. **28px 에서 계기가 선다는 뜻이 아니다** |

---

## ✅ 그래도 선 것 — 11회차와 이어 붙여 읽으면

| | |
|---|---|
| **계기가 두 표본에서 다 섰다** | 11회차 B(발목 대리, ρ 0.835) · 12회차 B(±1px 섭동 0.0%) |
| **표본 관문이 일했다** | 11회차는 주 층 **1프레임**, 12회차는 **198프레임**. 관문을 먼저 돌린 것이 그 차이다 |
| **불일치가 근소하지 않다** | `margin` 중앙 **1.687** 키높이 — 사람 키의 1.7배 떨어진 사람을 고르고 있다. **계기 해상도 문제가 아니다** |
| 🔴 **그런데 기전이 안 맞으면 (나)로 안 적는다** | 이 회차의 값이 여기 있다. `M`·`margin` 만 봤으면 **「선택이 틀린다」로 결론 내고 원근 때문이라고 적었을 것**이다. 둘 다 틀렸을 뻔했다 |

---

## 다음 한 걸음 — 🔴 **처방은 생각보다 비싸다**

기전이 「면적이 폭에 끌린다」이므로 **면적 대신 높이로 고르는 것**이 곧장
떠오른다. 🔴 **그런데 그건 `_largest_person_box` 의 **선택 규칙**을 바꾸는
것이고, 그 규칙이 selector 동작 기준이라 **B-1~B-6 이 전부 무효**가 된다.**
공짜가 아니다.

| | 후보 | 성질 |
|---|---|---|
| **(가)** | **폭이 넓은 이유를 가른다** — 자세인가, **겹친 사람 둘**인가 | 🔴 **권함.** 조사 회차라 `src/` 무변경이고, **처방이 갈린다**: 겹침이면 검출 후처리(NMS·병합) 문제이고 자세면 선택 규칙 문제다 |
| (나) | 면적 → 높이로 바꿔 보기 | **B-1~B-6 재실행**을 부른다. (가)의 답 없이 열지 않는다 |
| (다) | 공 근접을 **선택에 넣기** | 처방이고, **「공 쪽이 맞다」가 아직 안 섰다**(위) |
| (라) | 사람 판독 70장 (10회차의 (A)) | 표본 문제와 무관하게 여전히 열려 있다 |

🔴 **12회차는 진단이다. `src/` 를 안 고쳤고, 처방은 13회차에서 사전 등록을
새로 세운다.**
