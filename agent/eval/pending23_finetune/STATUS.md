# 어디까지 했고 다음에 무엇부터 (2026-09-18 10:05, **중단**)

🔴 **판정 회차가 아니다.** 사전 등록과 계기·데이터까지만 만들고 **사용자 판단으로
다음 주로 미뤘다.** 아래는 이어받는 사람이 **다시 만들지 않아도 되는 것**의 목록이다.

## 있는 것

| 파일 | 무엇 |
|---|---|
| `PREREGISTRATION.md` | 🔴 **정본.** 합격선(P-1~P-5)·선별 규칙·하지 말 것 |
| `generate.py` | 기준선(`--mode base`)·후보(`--mode candidates`) |
| `base_inside_pass.json` | **기준선 38문장** — 파인튜닝 전 모델, 그리디, 제품 경로 |
| `candidates_instep.json` | **후보 504개** (42자리 × 12, temp 0.9 · seed 20260918) |
| `curate.py` | `sheet`(선별용 대조표) · `apply --picks`(학습셋 + 점검 30개) |
| `finetune.py` | `train`(순수 torch LoRA) · `infer`(어댑터로 평가셋 38자리) |
| `compare.py` | BASE vs FT 나란히 + R1·R2·R3·고유 가짓수 |

전부 **돌려 본 것**이다 — `generate.py` 두 모드는 실제로 돌았고(9:52·9:57),
`curate.py sheet` 도 돌았다. `finetune.py`·`compare.py` 는 **아직 안 돌렸다.**

## 다음에 여기서부터

1. `uv run python eval/pending23_finetune/curate.py sheet` → 담당자가 방향
   대조로 고름 → `picks.json` (`{"slot_id": [번호, ...]}`)
2. `curate.py apply --picks picks.json` → `SPOTCHECK.md` 30개를 **사람이 점검**
   (🔴 합격선 **25/30**, 미달이면 학습 안 함)
3. `finetune.py train` → `finetune.py infer` → `compare.py` → 판독 → `RESULTS.md`

## 🔴 다음 주에 **먼저 판단할 것** — 후보 풀의 품질

선별을 시작하기 전에 `sheet` 를 훑다가 본 것이다. temp 0.9 표집에서 **1.2B 의
한국어가 눈에 띄게 무너진다.**

| 무엇 | 예 |
|---|---|
| 영어 혼입 | *"몸을 효과적으로 **prosecutions**으로 버티는"* · *"체중을 효과적으로 **utilized**한다"* · *"**phosphory**되지 않고"* |
| 낱말 깨짐 | **디트리머** · **디비전발** · **디프로미가** (전부 「디딤발」) |
| 등급 낱말 누출 | 0등급 자리에서 `[아쉬움]`·`[잘함]` — R1 거르개가 잡는다 |
| 형식 이탈 | 문장 대신 **리스트 두 줄**을 `evidence` 에 담음 |

🔴 **이대로 구우면 얻는 것이 「방향 일치」뿐이고 말은 더 딱딱해질 공산이 크다**
— 그러면 **P-5(고유 문장 가짓수 0.8배)에 걸려 불합격**이다. 그래서 다음 주
첫 판단은 **선별이 아니라 표집 온도**다:

- **temp 0.7 로 다시 뽑기** — 가짓수는 줄지만 깨진 한국어가 준다. 🔴 **다시
  뽑으면 사전 등록 2절의 「temp 0.9」를 고치는 것**이므로, **후보를 보기 전에**
  사전 등록에 적고 바꾼다
- 또는 **지금 풀 그대로** 가되 P-5 불합격을 **예상된 결과**로 두고 재 본다 —
  「표집 품질이 병목」이라는 것을 숫자로 남기는 값은 있다

## 안 건드린 것 — 그리고 **못 건드리게 막아 뒀다**

`src/`·루브릭·`pyproject.toml` **한 줄도 안 바뀌었다**(`git diff` 로 확인).
제품 경로·배포·B-6 재실행과 무관하고, 되돌릴 것이 없다. 어댑터도 아직 없다.

🔴 **사용자 지시 (2026-09-18): 「완성되기 전까지 에이전트에 관여하지 말 것.」**
문서에 적은 금지는 다음 세션이 안 읽으면 없는 것과 같아서 **검사로 박았다** —
`tests/test_finetune_stays_out_of_the_product.py`:

| 막는 것 | |
|---|---|
| `src/`·`scripts/`·`deploy/` 에 어댑터·LoRA 언급 | 제품 경로에 배선되는 것 |
| 의존성에 `peft`·`trl` | vLLM 쪽 torch 가 깨지는 것 (README 5-2) |
| 구운 어댑터가 커밋되는 것 | 배포 동기화를 타고 서버로 가는 것 (`.gitignore`) |

🔴 **이 검사를 「관련 없어 보인다」고 지우지 말 것.** 파인튜닝을 제품에 넣기로
**결정한 뒤** 미결 23번에 그 결정을 적고 함께 고친다 — 그래야 금지가 왜
풀렸는지가 남는다.
