# 결과 — 넓은 박스는 **자세**다 (미결 `ho` 18번 13회차)

2026.09.14 · 사전 등록 [`PREREGISTRATION.md`](PREREGISTRATION.md)(커밋 `205dd01`,
**측정 스크립트를 쓰기 전에** 굳혔다) · 🔴 **`src/`·`rubrics/` 0줄 변경**

```bash
cd agent && uv run python eval/pending18_wide/measure_wide.py
```

---

## 판정 — **(i) 자세.** 뿌리는 **선택 규칙**이다

| | 기준 | 결과 | |
|---|---|---|---|
| **A** | 12회차와 **같은 층**(불일치 163f) | **163f** | ✅ |
| **B** | 🔴 계기 사각지대 — 안 삼켰는데 `w/h ≥ 0.75` | **36/163 (22.1%)** | 보고 |
| **C** | `src/`·`rubrics/` 무변경 | **0줄** | ✅ |
| **D** | `SWALLOW` 네 값에서 판정 구간 불변 | 전부 **(자세)** | ✅ |
| **E** | `pytest -q` | **413 통과** | ✅ |
| **F** | 짝 기준 | **해당 없음** (`S` ≤ 30%) | — |

**`S` = 12.3%** (20/163). 고른 넓은 박스가 **다른 후보를 삼키고 있는 경우는
여덟에 하나**다. IoA 최대값의 중앙은 **0.19** — 대개 스치는 정도다.

| `SWALLOW` | 0.5 | 0.6 | **0.7** | 0.8 |
|---|---:|---:|---:|---:|
| `S` | 27.6% | 17.8% | **12.3%** | 7.4% |

**문턱을 0.5까지 내려도 27.6%** 로 (i) 쪽이다. 🔴 **문턱에 업혀 있지 않다.**

### 🔴 사각지대를 최악으로 쳐도 결론이 안 뒤집힌다

기준 B 가 센 **36프레임**(넓은데 삼킨 것이 0)은 「겹쳤는데 **개별 사람이 따로
검출되지 않아** 삼킬 것이 없는 경우」일 수 있다 — 사전 등록 1절이 미리 적어 둔
사각지대다. **그 36개를 전부 겹침으로 쳐도**

```
(20 + 36) / 163 = 34.4%
```

**「둘 다」 구간의 아래쪽에 겨우 닿는다.** 즉 **최악의 가정에서도 자세가
다수(65.6%)** 다. 🔴 **사각지대가 결론을 뒤집을 만큼 크지 않다는 것이 이
회차에서 실제로 확인된 것**이고, 사각지대를 세어 두지 않았으면 이 문장을 쓸 수
없었다.

---

## 🔴 내 예측이 또 틀렸다 — **두 회차 연속이다**

사전 등록에 **「둘 다(30~70%)가 나올 것」**이라고 박았다. **12.3% 로 (i) 자세다.**

근거로 적었던 것은 *"고른 박스의 `w/h` 중앙이 **0.61** 로 선 사람(0.3~0.5)보다는
넓지만 두 사람을 덮는 값(≥1.0)에는 한참 못 미친다 → 두 원인이 섞여 있다"* 였다.
🔴 **전제는 맞았는데 결론이 틀렸다.** `w/h` 0.61 은 겹침이 **섞여 있다**는 뜻이
아니라 **겹침이 거의 없다**는 뜻이었다. 실제로 `w/h ≥ 1.0` 인 프레임은
**8/163** 뿐이다.

사전 등록에 **「이 근거는 집계값 하나에서 온 것이라 약하다」**고 적어 둔 것이
맞았다. **12회차에서도 예측이 틀렸고 이번에도 틀렸다** — 🔴 **집계값 하나로
기전을 추정하는 내 습관이 문제다.** 다음부터는 **예측을 적을 때 그 예측이
어떤 분포에서 나왔는지**를 함께 적는다(중앙값 하나가 아니라 꼬리까지).

**다만 예측이 틀린 것이 회차를 망치지는 않았다** — 판정은 사전 등록 규칙대로
났고, 예측은 **옆에 붙여 둔** 것이지 판정에 안 들어갔다(9회차에서 배운 형태).

---

## 눈으로 본 것 — 🔴 **판정에 안 쓴다** (사전 등록 5절)

가장 넓은 6개를 그려 봤다(`/tmp` 에서만 — 🔴 **방송 프레임은 저장소에 안 넣는다**).

| `w/h` | 삼킴 | 무엇이었나 |
|---:|---:|---|
| **3.39** | 0 | 🔴 **땅에 누운 선수** — 슬라이딩 태클/넘어짐. **몸이 가로로 누워 있다** |
| 1.55 | 0 | 🔴 **엎드린 선수** — 손을 짚고 웅크림 |
| 1.26 | 0 | 기울어진 선수 (작아서 단정 못 함) |
| 1.17 | **2** | ✅ **진짜 겹침** — 엉킨 선수 무리. 유일하게 삼킨 예 |
| 1.10 | 0 | 팔 벌리고 달리는 선수 |
| 1.09 | 0 | 위와 같은 장면의 다음 프레임 |

**「자세」가 막연한 말이 아니었다 — 상당수가 「가로로 누운 몸」이다.**

🔴 **여기서 처방의 방향이 한 칸 더 보인다.** 면적으로 고르면 **누운 사람이
이긴다** — 가로로 길어 면적이 크기 때문이다. 그리고 **누운 사람은 공을 다루고
있을 리가 거의 없다.** 높이로 고르면 누운 사람은 **높이가 작아져** 밀린다.

🔴 **그런데 이것은 6프레임을 본 인상이지 측정이 아니다.** 「누웠는가」를
**`w/h` 로 세는 것**은 그 자체가 가설이라, **처방 회차의 사전 등록에서
수치로 고정하고 전수로 센다.**

---

## 🔴 말하지 않는 것

- **제품 성능이 아니다.** 방송 중계이고 제품 입력은 휴대폰이다(12회차 1절)
- **「공 쪽이 맞다」가 아니다.** 공 근접은 라벨이 아니라 물리적 개연성이다
- **`S` 12.3% 가 「겹침은 없다」가 아니다** — 20프레임에서 실제로 일어났고
  그중 하나는 눈으로도 확인했다. **주된 원인이 아니라는 것**까지다
- 층이 **9클립 163프레임**이다. 🔴 **안 쓴 표본이 없다**(12회차와 같은 층)

---

## 다음 한 걸음 — **처방 회차이고, 비싸다**

세 회차가 여기까지 왔다: 공과 우리 규칙이 **다른 사람을 가리키고**(12회차
`M` 17.7%), 그 이유는 **면적이 폭에 끌리는 것**이며(12회차), 폭의 뿌리는
**겹침이 아니라 자세**다(이 회차).

| | 후보 | 성질 |
|---|---|---|
| **(가)** | `_largest_person_box` 의 **면적 → 높이** | 🔴 **selector 동작 기준을 바꾼다 — B-1~B-6 이 전부 무효**가 된다. **공짜가 아니다.** 사전 등록에 **재실행 계획**까지 넣어야 한다 |
| (나) | **누운 박스만 빼기** (`w/h` 상한) | 새 상수가 하나 붙는다. 🔴 **그리고 「누웠다」를 `w/h` 로 정의하는 것 자체가 가설**이다 |
| (다) | 공 근접을 **선택에 넣기** | 「공 쪽이 맞다」가 아직 안 섰다 |
| (라) | 사람 판독 70장 (10회차의 (A)) | 여전히 열려 있다 |

🔴 **(가)와 (나) 중 무엇을 고르든 처방이고, 이 회차는 진단이다.** `src/` 를
안 고쳤다. 14회차에서 사전 등록을 새로 세우고, **거기에는 「무엇이 무효가
되는가」와 「무엇을 다시 돌리는가」가 반드시 들어간다.**
