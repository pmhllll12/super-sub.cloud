# 카드·스쿼드 서버 연결 1단계(데이터 계층) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱이 `GET /me`·`GET /me/card`·`GET /teams/{id}/squad`·`GET /cards/{slug}` 를 실제로 읽어, 웹에서 만든 팀·카드·스쿼드 자리가 앱 홈 판에 그대로 뜨게 한다.

**Architecture:** `flutter/CLAUDE.md` 의 뼈대 그대로 — 화면은 리포지토리 인터페이스만 알고, Mock/API 교체는 `*_providers.dart` 한 줄이다. HTTP 공통(토큰·UTF-8 디코딩·에러 `code`·`Retry-After`)은 지금 `ApiAuthRepository` 안에 갇혀 있으므로 `core/network/api_client.dart` 로 끌어올려 셋이 나눠 쓴다. 자리 배치는 웹 `seatsFromSquad` 를 **순수 함수**로 옮겨 테스트로 잠근다.

**Tech Stack:** Flutter · Dart · Riverpod 3 · `http` 패키지 · `flutter_test`

**Spec:** `flutter/docs/2026-09-21-카드-스쿼드-서버연결-design.md`

## Global Constraints

- 🔴 화면·컨트롤러에서 구현체(`Mock…()` / `Api…()`)를 **직접 만들지 않는다.** 교체 지점은 `*_providers.dart` 뿐이다
- 🔴 화면에서 `useMockProvider` 를 읽어 분기하지 않는다
- 🔴 Mock 은 **일부러 느리다** — `Duration(milliseconds: 300)` 지연을 유지한다. 즉시 성공하게 고치면 로딩·오류 화면을 안 만들게 된다
- 🔴 계약 테스트(`test/contract/*_contract.dart`)에는 **프로토콜의 성질만** 둔다. Mock 전용 의무(지연 하한)·개발용 편의는 구현체별 테스트 파일로 내린다
- 🔴 새 리포지토리를 만들면 계약 테스트도 같이 만들고 **Mock·API 양쪽에 물린다**
- 🔴 무한 애니메이션이 있는 화면의 위젯 테스트에서 `pumpAndSettle` 금지 — `pumpWidget` → 트리거 → `pump(500ms)`
- 🔴 API 주소·계정 ID 등 인프라 식별자를 코드·문서에 쓰지 않는다. 주소는 `lib/core/network/api_config.dart` 의 `apiBaseUrl` 하나뿐이고 실행 시 `--dart-define` 으로 덮는다
- 🔴 카드에 수치(점수·등급·진행률)를 그리지 않는다(부록 D.5)
- 확인 명령: `flutter analyze` 는 **0건**, `flutter test` 는 전부 통과

---

### Task 1: `ApiClient` — HTTP 공통을 끌어올린다

지금 토큰 보관·UTF-8 디코딩·에러 `code` 파싱이 `ApiAuthRepository` 안에만 있다. 카드·스쿼드가 같은 것을 필요로 하므로 복붙하면 **세 벌**이 되고, 특히 「`response.body` 를 쓰면 한글이 깨진다」는 함정을 한 곳이라도 빠뜨리면 거기서만 글자가 깨진다.

**Files:**
- Create: `lib/core/network/api_client.dart`
- Modify: `lib/features/auth/data/auth_repository_api.dart` (전체 — `ApiClient` 위로 옮긴다)
- Test: `test/core/api_client_test.dart`
- 기존 테스트가 회귀를 잡는다: `test/features/auth/auth_repository_api_test.dart`

**Interfaces:**
- Produces:
  - `class ApiException implements Exception` — `final String message; final String? code; final int? retryAfter; final int? status;`
  - `class ApiClient` — `ApiClient({TokenStore? tokens, http.Client? client})`,
    `Future<Map<String, dynamic>> get(String path, {bool authorized = true})`,
    `Future<Map<String, dynamic>> post(String path, [Map<String, dynamic>? body])`,
    `Future<Map<String, dynamic>> patch(String path, Map<String, dynamic> body)`,
    `Future<void> delete(String path)`,
    `Future<void> useToken(String token)`, `Future<void> clearToken()`,
    `Future<String?> loadToken()`
  - `final apiClientProvider = Provider<ApiClient>((ref) => ApiClient());`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test/core/api_client_test.dart`:

```dart
import 'dart:convert';

import 'package:flutter_test/flutter_test.dart';
import 'package:http/http.dart' as http;
import 'package:http/testing.dart';
import 'package:super_sub/core/network/api_client.dart';
import 'package:super_sub/features/auth/data/token_store.dart';

class _MemoryTokenStore implements TokenStore {
  String? _value;
  @override
  Future<String?> read() async => _value;
  @override
  Future<void> write(String token) async => _value = token;
  @override
  Future<void> clear() async => _value = null;
}

void main() {
  test('본문을 UTF-8 로 푼다 — charset 이 없어도', () async {
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((_) async => http.Response.bytes(
            utf8.encode('{"nickname":"홍길동"}'),
            200,
            headers: {'content-type': 'application/json'},
          )),
    );

    final body = await client.get('/me');

    expect(body['nickname'], equals('홍길동'));
  });

  test('토큰이 있으면 Authorization 헤더를 싣는다', () async {
    String? seen;
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((req) async {
        seen = req.headers['Authorization'];
        return http.Response('{}', 200);
      }),
    );
    await client.useToken('t-123');

    await client.get('/me');

    expect(seen, equals('Bearer t-123'));
  });

  test('authorized: false 면 토큰이 있어도 헤더를 안 싣는다', () async {
    String? seen;
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((req) async {
        seen = req.headers['Authorization'];
        return http.Response('{}', 200);
      }),
    );
    await client.useToken('t-123');

    await client.get('/cards/abc', authorized: false);

    expect(seen, isNull);
  });

  test('실패 응답의 code·status 를 ApiException 에 싣는다', () async {
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((_) async => http.Response(
            '{"error":{"code":"CARD_NOT_FOUND","message":"없다"}}',
            404,
          )),
    );

    await expectLater(
      client.get('/me/card'),
      throwsA(isA<ApiException>()
          .having((e) => e.code, 'code', 'CARD_NOT_FOUND')
          .having((e) => e.status, 'status', 404)),
    );
  });

  test('Retry-After 가 0 이하거나 없으면 최소 1초로 올린다', () async {
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((_) async => http.Response(
            '{"error":{"code":"TOO_MANY_REQUESTS","message":"곧"}}',
            429,
            headers: {'retry-after': '0'},
          )),
    );

    await expectLater(
      client.get('/me'),
      throwsA(isA<ApiException>().having((e) => e.retryAfter, 'retryAfter', 1)),
    );
  });

  test('본문이 비어도(204) delete 는 터지지 않는다', () async {
    final client = ApiClient(
      tokens: _MemoryTokenStore(),
      client: MockClient((_) async => http.Response('', 204)),
    );

    await expectLater(client.delete('/me/card'), completes);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/core/api_client_test.dart`
Expected: FAIL — `api_client.dart` 가 없다는 컴파일 오류

- [ ] **Step 3: `ApiClient` 를 만든다**

`lib/core/network/api_client.dart`:

```dart
import 'dart:convert';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:http/http.dart' as http;

import '../../features/auth/data/token_store.dart';
import 'api_config.dart';

/// 서버가 준 실패. 계약은 늘 `{"error": {"code", "message"}}` 다.
///
/// 🔴 **[status] 를 싣는 이유**: 404 를 「오류」가 아니라 「없음(null)」으로
/// 다뤄야 하는 자리가 있다(`GET /me/card` · `GET /teams/{id}/squad`).
/// `code` 만으로 가르면 서버가 code 를 안 주는 응답에서 못 가른다.
class ApiException implements Exception {
  const ApiException(this.message, {this.code, this.retryAfter, this.status});

  final String message;
  final String? code;

  /// 429 일 때 몇 초 기다려야 하는가. 429 가 아니거나 헤더가 없으면 `null`.
  final int? retryAfter;
  final int? status;

  @override
  String toString() => message;
}

/// 백엔드를 부르는 공통 창구 — 토큰 보관 · 헤더 · 디코딩 · 오류 변환.
///
/// 🔴 **리포지토리마다 http 를 직접 부르지 않는다.** 아래 두 함정이 한 곳이라도
/// 빠지면 거기서만 글자가 깨지거나 429 잠금이 안 걸린다.
class ApiClient {
  ApiClient({TokenStore? tokens, http.Client? client})
      : _tokens = tokens ?? const SecureTokenStore(),
        _client = client ?? http.Client();

  final TokenStore _tokens;
  final http.Client _client;
  String? _token;

  /// 저장소의 토큰을 메모리로 올린다(앱을 켤 때 한 번). 없으면 `null`.
  Future<String?> loadToken() async => _token = await _tokens.read();

  /// 토큰을 메모리와 저장소 **둘 다**에 둔다 — 지금 요청에도 써야 하고,
  /// 다음에 켤 때도 있어야 한다.
  Future<void> useToken(String token) async {
    _token = token;
    await _tokens.write(token);
  }

  Future<void> clearToken() async {
    _token = null;
    await _tokens.clear();
  }

  Future<Map<String, dynamic>> get(String path, {bool authorized = true}) async {
    final res = await _send(
      () => _client.get(_uri(path), headers: _headers(authorized: authorized)),
    );
    return _decode(res);
  }

  Future<Map<String, dynamic>> post(
    String path, [
    Map<String, dynamic>? body,
  ]) async {
    final res = await _send(
      () => _client.post(
        _uri(path),
        headers: _headers(),
        body: body == null ? null : jsonEncode(body),
      ),
    );
    return _decode(res);
  }

  Future<Map<String, dynamic>> patch(
    String path,
    Map<String, dynamic> body,
  ) async {
    final res = await _send(
      () => _client.patch(_uri(path), headers: _headers(), body: jsonEncode(body)),
    );
    return _decode(res);
  }

  Future<void> delete(String path) async {
    final res = await _send(
      () => _client.delete(_uri(path), headers: _headers()),
    );
    _decode(res);
  }

  Uri _uri(String path) => Uri.parse('$apiBaseUrl$path');

  Map<String, String> _headers({bool authorized = true}) => {
        'Content-Type': 'application/json',
        if (authorized && _token != null) 'Authorization': 'Bearer $_token',
      };

  Future<http.Response> _send(Future<http.Response> Function() request) async {
    try {
      return await request();
    } on http.ClientException catch (e) {
      throw ApiException('서버에 연결할 수 없습니다: ${e.message}');
    }
  }

  Map<String, dynamic> _decode(http.Response response) {
    /* 🔴 **`response.body` 를 쓰지 않는다.** `http` 는 헤더에 `charset` 이
       없으면 **latin1 로** 푼다. FastAPI 는 `application/json` 만 주고 charset 을
       안 붙이므로 한글 닉네임과 오류 문구가 깨져서 들어온다. 바이트를 직접
       UTF-8 로 푼다 — 서버가 charset 을 주든 안 주든 맞는 방식이다. */
    final decoded = response.bodyBytes.isEmpty
        ? <String, dynamic>{}
        : jsonDecode(utf8.decode(response.bodyBytes)) as Map<String, dynamic>;
    if (response.statusCode >= 200 && response.statusCode < 300) {
      return decoded;
    }
    final error = decoded['error'] as Map<String, dynamic>?;
    // 🔴 서버가 값을 안 주면 0 이 아니라 최소 1초 — 0 이면 잠금이 곧바로 풀려
    // "429 직후 재요청이 안 나간다"가 깨진다(웹과 같은 판단).
    final raw = response.headers['retry-after'];
    final parsed = raw == null ? null : int.tryParse(raw);
    throw ApiException(
      (error?['message'] as String?) ?? '알 수 없는 오류 (${response.statusCode})',
      code: error?['code'] as String?,
      retryAfter: parsed == null ? null : (parsed > 0 ? parsed : 1),
      status: response.statusCode,
    );
  }
}

/// 세 리포지토리(인증 · 카드 · 스쿼드)가 **같은 한 벌**을 나눠 쓴다 — 로그인이
/// 저장한 토큰을 나머지가 그대로 쓰기 때문이다.
final apiClientProvider = Provider<ApiClient>((ref) => ApiClient());
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `flutter test test/core/api_client_test.dart`
Expected: PASS (6개)

- [ ] **Step 5: `ApiAuthRepository` 를 `ApiClient` 위로 옮긴다**

`lib/features/auth/data/auth_repository_api.dart` — 머리말 주석은 그대로 두고, 몸통만 바꾼다. **`AuthException` 은 남긴다**(기존 화면·계약 테스트가 그 타입을 본다) — `ApiException` 을 잡아 감싼다.

```dart
class ApiAuthRepository implements AuthRepository {
  ApiAuthRepository({ApiClient? client}) : _api = client ?? ApiClient();

  final ApiClient _api;
  Session? _current;

  @override
  Future<Session> login({
    required String email,
    required String password,
  }) async {
    final tokenBody = await _call(
      () => _api.post('/auth/login', {'email': email, 'password': password}),
    );
    await _api.useToken(tokenBody['access_token'] as String);
    return _current = Session(user: await _fetchMe());
  }

  // signup · loginWithGoogle 도 같은 모양으로 _call 을 통과시킨다.

  @override
  Future<void> logout() async {
    _current = null;
    // 🔴 저장소에서도 지운다 — 안 지우면 다음에 켤 때 로그아웃한 계정으로
    //    되돌아간다.
    await _api.clearToken();
  }

  @override
  Future<Session?> restoreSession() async {
    if (_current != null) return _current;
    if (await _api.loadToken() == null) return null;
    try {
      return _current = Session(user: await _fetchMe());
    } on AuthException catch (e) {
      /* 🔴 **토큰을 버리는 것은 「이 토큰이 못 쓴다」고 서버가 말했을 때뿐이다.**
         비행기 모드나 서버가 잠깐 죽은 것으로는 안 버린다 — 그때도 버리면
         잠깐 끊긴 것 때문에 사람이 로그아웃된다. */
      if (e.code == 'UNAUTHORIZED' || e.code == 'INVALID_TOKEN') {
        await _api.clearToken();
      }
      return null;
    }
  }

  /// `ApiException` 을 이 컨텍스트의 [AuthException] 으로 옮긴다 — 화면과
  /// 계약 테스트가 보는 타입을 안 바꾸기 위해서다.
  Future<Map<String, dynamic>> _call(
    Future<Map<String, dynamic>> Function() request,
  ) async {
    try {
      return await request();
    } on ApiException catch (e) {
      throw AuthException(e.message, code: e.code, retryAfter: e.retryAfter);
    }
  }
}
```

`_fetchMe()` 도 `_call(() => _api.get('/me'))` 를 쓰게 바꾼다. `token_store.dart` 임포트와 `_token`·`_tokens`·`_post`·`_get`·`_send`·`_headers`·`_decode` 는 **지운다**(`ApiClient` 로 갔다).

- [ ] **Step 6: auth 테스트가 그대로 통과하는지 확인한다 — 이것이 리팩터의 안전망이다**

Run: `flutter test test/features/auth/`
Expected: PASS (전부). 🔴 기존 테스트가 `http.Client` 를 주입하고 있으면 `ApiAuthRepository(client: ApiClient(client: mockHttp, tokens: …))` 형태로 고친다 — **테스트의 기대값은 바꾸지 않는다.** 기대값을 고쳐야 통과한다면 그것은 리팩터가 동작을 바꿨다는 뜻이다

- [ ] **Step 7: 전체를 돌리고 커밋한다**

Run: `flutter analyze && flutter test`
Expected: analyze 0건, 테스트 전부 통과

```bash
git add lib/core/network/api_client.dart lib/features/auth/data/auth_repository_api.dart test/core/api_client_test.dart test/features/auth/
git commit -m "refactor(flutter): HTTP 공통을 ApiClient 로 — 카드·스쿼드가 나눠 쓴다"
```

---

### Task 2: `GET /me` 의 `teams[]` 를 싣는다

앱은 지금 내가 어느 팀인지 모른다. **새 경로가 필요 없다** — `GET /me` 가 이미 주는데 `_fetchMe()` 가 버리고 있다.

**Files:**
- Create: `lib/features/auth/data/models/team_membership.dart`
- Modify: `lib/features/auth/data/models/app_user.dart` (`teams` 필드 추가)
- Modify: `lib/features/auth/data/auth_repository_api.dart` (`_fetchMe` 파싱)
- Modify: `lib/features/auth/data/auth_repository_mock.dart` (MockDb 에서 채운다)
- Test: `test/features/auth/team_membership_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `ApiClient.get`
- Produces:
  - `class TeamMembership { final String teamId, name, region, sportCode, role; final DateTime joinedAt; }`
  - `AppUser.teams` → `List<TeamMembership>` (기본값 `const []`)
  - `TeamMembership.fromJson(Map<String, dynamic>)`
  - `AppUser.ownedTeamId` → `String?` — `role == 'owner'` 인 첫 팀. 주장만 할 수 있는 일(스쿼드 만들기·등재)의 대상이다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test/features/auth/team_membership_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/auth/data/models/team_membership.dart';

void main() {
  test('GET /me 의 teams 항목을 읽는다', () {
    final m = TeamMembership.fromJson(const {
      'team_id': '9a2e',
      'name': '번개FC',
      'region': '서울 강남',
      'sport_code': 'football',
      'role': 'member',
      'joined_at': '2026-07-01T00:00:00Z',
    });

    expect(m.teamId, '9a2e');
    expect(m.name, '번개FC');
    expect(m.sportCode, 'football');
    expect(m.role, 'member');
    expect(m.joinedAt, DateTime.utc(2026, 7, 1));
  });
}
```

그리고 `test/features/auth/auth_repository_api_test.dart` 에 한 건을 더한다:

```dart
  test('GET /me 의 teams 를 AppUser 에 싣는다', () async {
    // MockClient 가 /auth/login 에 토큰을, /me 에 teams 한 건을 준다.
    // (이 파일의 기존 MockClient 구성 방식을 그대로 따른다)
    final session = await repo.login(email: 'a@b.test', password: 'x');

    expect(session.user.teams, hasLength(1));
    expect(session.user.teams.first.name, '번개FC');
    expect(session.user.ownedTeamId, isNull); // role 이 member 다
  });
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/auth/`
Expected: FAIL — `team_membership.dart` 없음

- [ ] **Step 3: 모델을 만들고 `AppUser` 에 싣는다**

`lib/features/auth/data/models/team_membership.dart`:

```dart
/// `GET /me` 의 `teams[]` 한 항목 — **내가 지금 속한 팀**이다.
///
/// 🔴 서버가 `left_at` 이 널인 행만 추려서 주므로 나간 팀은 애초에 안 온다
/// (계약 「`teams`는 `team_member`에서 `left_at`이 널인 행만 추린다」).
class TeamMembership {
  const TeamMembership({
    required this.teamId,
    required this.name,
    required this.region,
    required this.sportCode,
    required this.role,
    required this.joinedAt,
  });

  factory TeamMembership.fromJson(Map<String, dynamic> json) => TeamMembership(
        teamId: json['team_id'] as String,
        name: json['name'] as String,
        region: json['region'] as String,
        sportCode: json['sport_code'] as String,
        role: json['role'] as String,
        joinedAt: DateTime.parse(json['joined_at'] as String),
      );

  final String teamId;
  final String name;
  final String region;
  final String sportCode;

  /// `owner` · `member`. 🔴 **문자열 그대로 둔다** — 값이 늘 때 앱을 고치지
  /// 않으려는 것이고, 서버도 같은 이유로 enum 을 안 쓴다.
  final String role;
  final DateTime joinedAt;

  bool get isOwner => role == 'owner';
}
```

`AppUser` 에 `final List<TeamMembership> teams;` 를 더하고 생성자에 `this.teams = const []`, `copyWith`·`==`·`hashCode` 에도 반영한다(`Object.hash` 는 리스트를 얕게 보므로 `Object.hashAll` 로 팀을 따로 섞는다).

```dart
  /// 내가 주장인 팀 — 스쿼드를 **만들고 등재할 수 있는** 팀이다.
  /// 🔴 주장이 아닌 팀의 스쿼드는 **읽기만** 된다(계약 3-7절 권한표).
  String? get ownedTeamId {
    for (final t in teams) {
      if (t.isOwner) return t.teamId;
    }
    return null;
  }

  /// 판을 그릴 대상 팀 — 주장인 팀이 우선, 없으면 속한 첫 팀, 그것도 없으면 null.
  String? get primaryTeamId => ownedTeamId ?? (teams.isEmpty ? null : teams.first.teamId);
```

- [ ] **Step 4: `_fetchMe` 와 Mock 을 잇는다**

`auth_repository_api.dart` 의 `_fetchMe`:

```dart
  Future<AppUser> _fetchMe() async {
    final body = await _call(() => _api.get('/me'));
    return AppUser(
      id: body['id'] as String,
      email: body['email'] as String,
      nickname: body['nickname'] as String,
      createdAt: DateTime.parse(body['created_at'] as String),
      // 🔴 옛 서버는 이 칸을 안 줄 수 있다 — 없으면 빈 목록이다.
      teams: ((body['teams'] as List<dynamic>?) ?? const [])
          .map((e) => TeamMembership.fromJson(e as Map<String, dynamic>))
          .toList(growable: false),
    );
  }
```

`auth_repository_mock.dart` 는 `MockDb.teamMembers`·`teams` 를 엮어 같은 모양을 만든다. `MockDb.playerId` 는 `t-thunder` 의 **member**, `MockDb.managerId` 는 **manager** 다 — 기존 시드의 `TeamRole` 을 `'owner'`/`'member'` 문자열로 옮긴다(`TeamRole.manager` → `'owner'`).

- [ ] **Step 5: 통과를 확인하고 커밋한다**

Run: `flutter analyze && flutter test`
Expected: analyze 0건, 전부 통과

```bash
git add lib/features/auth/ test/features/auth/
git commit -m "feat(flutter): GET /me 의 teams 를 AppUser 에 싣는다 — 내 팀을 안다"
```

---

### Task 3: 카드 — 모델 · 리포지토리 · 계약 테스트 · 두 구현체

**Files:**
- Create: `lib/features/card/data/models/player_card.dart`
- Create: `lib/features/card/data/card_repository.dart`
- Create: `lib/features/card/data/card_repository_mock.dart`
- Create: `lib/features/card/data/card_repository_api.dart`
- Create: `lib/features/card/data/card_providers.dart`
- Test: `test/contract/card_repository_contract.dart`
- Test: `test/features/card/card_repository_mock_test.dart`
- Test: `test/features/card/card_repository_api_test.dart`
- Test: `test/features/card/player_card_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `ApiClient`·`ApiException`, Task 2 의 `useMockProvider`(기존)
- Produces:
  - `class CardTitle { final String code, label, category; final DateTime grantedAt; }`
  - `class CardStyle` — 이번 단계에서는 **원본 맵을 들고만 있는다**(2단계가 읽는다):
    `CardStyle.fromJson(Map<String, dynamic>)`, `final Map<String, dynamic> raw;`
  - `class PlayerCard { final String id, publicSlug; final String nickname; final String? tagline; final CardStyle? style; final String? photoUrl; final List<CardTitle> titles; }`
    · `PlayerCard.fromJson(Map<String, dynamic>)`
  - `abstract class CardRepository` — `Future<PlayerCard?> myCard()`, `Future<PlayerCard> createMyCard()`, `Future<PlayerCard?> cardBySlug(String slug)`
  - `final cardRepositoryProvider = Provider<CardRepository>(…)`
  - `final myCardProvider = FutureProvider<PlayerCard?>(…)`

- [ ] **Step 1: 모델 테스트를 쓴다**

`test/features/card/player_card_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/card/data/models/player_card.dart';

void main() {
  const wire = {
    'id': '7b4d',
    'public_slug': 'hong-gildong-4f2a',
    'user': {'id': '3f1c', 'nickname': '홍길동'},
    'titles': [
      {
        'code': 'sharp_shooter',
        'label': '슈팅이 매서운',
        'category': '강점',
        'granted_at': '2026-08-20T12:00:00Z',
      }
    ],
    'tagline': 'THREE LUNGS',
    'style': {'bg': '#91ea92', 'brush': 3},
  };

  test('계약 응답을 읽는다', () {
    final card = PlayerCard.fromJson(wire);

    expect(card.id, '7b4d');
    expect(card.publicSlug, 'hong-gildong-4f2a');
    expect(card.nickname, '홍길동');
    expect(card.tagline, 'THREE LUNGS');
    expect(card.titles.single.label, '슈팅이 매서운');
    expect(card.style!.raw['brush'], 3);
  });

  test('tagline·style 이 null 이면 null 로 둔다 — 지어내지 않는다', () {
    final card = PlayerCard.fromJson({
      ...wire,
      'tagline': null,
      'style': null,
    });

    expect(card.tagline, isNull);
    expect(card.style, isNull);
  });

  test('titles 가 없어도 빈 목록이다', () {
    final json = Map<String, dynamic>.from(wire)..remove('titles');
    expect(PlayerCard.fromJson(json).titles, isEmpty);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/card/player_card_test.dart`
Expected: FAIL — 파일 없음

- [ ] **Step 3: 모델을 만든다**

`lib/features/card/data/models/player_card.dart`:

```dart
/// 카드에 붙은 호칭. 🔴 **받은 것만** 그린다 — `earned: false` 같은 미달 표식을
/// 만들지 않는다(웹과 같은 규칙).
class CardTitle {
  const CardTitle({
    required this.code,
    required this.label,
    required this.category,
    required this.grantedAt,
  });

  factory CardTitle.fromJson(Map<String, dynamic> json) => CardTitle(
        code: json['code'] as String,
        label: json['label'] as String,
        category: json['category'] as String,
        grantedAt: DateTime.parse(json['granted_at'] as String),
      );

  final String code;
  final String label;
  final String category;
  final DateTime grantedAt;
}

/// 카드 꾸미기. **1단계에서는 원본 맵을 들고만 있는다** — 색·자국·사진을 실제로
/// 그리는 것은 2단계(`…-design.md` 3절)다.
///
/// 🔴 **그럼에도 지금 필요하다**: 별명 규칙이 「`style` 이 있는가」로 갈린다
/// (`tagline == null && style != null` 이면 **일부러 지운 것**).
class CardStyle {
  const CardStyle(this.raw);

  factory CardStyle.fromJson(Map<String, dynamic> json) => CardStyle(json);

  final Map<String, dynamic> raw;
}

/// `GET /me/card` · `GET /cards/{slug}` 의 응답.
///
/// ⚠️ 공개 응답(`/cards/{slug}`)에는 `id` 가 없다 — 공개해도 되는 것만 담기
/// 때문이다. 그래서 [id] 가 `null` 일 수 있다.
class PlayerCard {
  const PlayerCard({
    required this.publicSlug,
    required this.nickname,
    this.id,
    this.tagline,
    this.style,
    this.photoUrl,
    this.titles = const [],
  });

  factory PlayerCard.fromJson(Map<String, dynamic> json) {
    final user = json['user'] as Map<String, dynamic>?;
    final style = json['style'] as Map<String, dynamic>?;
    return PlayerCard(
      id: json['id'] as String?,
      publicSlug: json['public_slug'] as String,
      nickname: (user?['nickname'] as String?) ?? '',
      tagline: json['tagline'] as String?,
      style: style == null ? null : CardStyle.fromJson(style),
      // 🔴 그릴 주소는 `style.photo_key` 가 아니라 이것이다(사전 서명·만료).
      photoUrl: json['photo_url'] as String?,
      titles: ((json['titles'] as List<dynamic>?) ?? const [])
          .map((e) => CardTitle.fromJson(e as Map<String, dynamic>))
          .toList(growable: false),
    );
  }

  final String? id;
  final String publicSlug;
  final String nickname;
  final String? tagline;
  final CardStyle? style;
  final String? photoUrl;
  final List<CardTitle> titles;
}

/// 카드에 그릴 큰 글자 — 🔴 「비웠다」와 「안 정했다」를 **`style` 유무로** 가른다.
///
/// 서버는 `tagline: null` 과 `"   "` 를 똑같이 「안 정한 상태」로 만들어서
/// `tagline` 만으로는 못 가른다. `style` 이 있다는 것은 **저장을 한 번 거쳤다**는
/// 뜻이고 그때 글자 칸에는 카드에 보이던 값이 들어 있었으니, `style` 이 있는데
/// `tagline` 이 비었으면 **지운 것**이다(웹 `aliasOf()` 와 한 글자도 다르면 안 된다).
String aliasOf(PlayerCard card) =>
    card.tagline ?? (card.style != null ? '' : kDefaultCardAlias);
```

`kDefaultCardAlias`(`'THREE LUNGS'`)는 이미 `player_card_view.dart` 에 있다 — **거기서 임포트해 쓰고 새로 만들지 않는다**(두 벌이 되면 갈린다).

- [ ] **Step 4: 모델 테스트 통과를 확인한다**

Run: `flutter test test/features/card/player_card_test.dart`
Expected: PASS (3개)

- [ ] **Step 5: 계약 테스트를 쓴다 — 구현체가 아니라 인터페이스의 시험이다**

`test/contract/card_repository_contract.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/card/data/card_repository.dart';

/// CardRepository 의 모든 구현체가 지켜야 하는 계약.
///
/// 🔴 여기에는 **프로토콜의 성질만** 둔다. Mock 에만 있는 의무(지연 하한)는
/// 구현체별 테스트로 내린다 — 계약에 섞으면 API 구현체가 통과할 수 없다.
///
/// [build] 는 매 테스트마다 깨끗한 구현체를 만든다.
/// [slugWithCard] 는 그 구현체에 **실제로 존재하는** 카드의 공개 슬러그다.
void runCardRepositoryContract(
  String name,
  CardRepository Function() build, {
  required String slugWithCard,
}) {
  group('$name — CardRepository 계약', () {
    late CardRepository repo;

    setUp(() => repo = build());

    test('카드가 없으면 예외가 아니라 null 이다', () async {
      // 🔴 이것이 이 계약의 핵심이다. 404 CARD_NOT_FOUND 는 오류가 아니라
      //    「아직 안 만들었다」이고, 화면은 그때 빈 카드를 그린다.
      expect(await repo.myCard(), isNull);
    });

    test('만들면 카드가 생기고 슬러그가 있다', () async {
      final card = await repo.createMyCard();
      expect(card.publicSlug, isNotEmpty);
      expect(await repo.myCard(), isNotNull);
    });

    test('두 번 만들어도 슬러그가 안 바뀐다 — 멱등', () async {
      // 🔴 이미 공유한 주소가 죽으면 안 된다(계약 POST /me/card).
      final first = await repo.createMyCard();
      final second = await repo.createMyCard();
      expect(second.publicSlug, equals(first.publicSlug));
    });

    test('남의 카드를 공개 슬러그로 읽는다', () async {
      final card = await repo.cardBySlug(slugWithCard);
      expect(card, isNotNull);
      expect(card!.publicSlug, equals(slugWithCard));
    });

    test('없는 슬러그는 null 이다', () async {
      expect(await repo.cardBySlug('no-such-slug-0000'), isNull);
    });
  });
}
```

- [ ] **Step 6: 인터페이스와 Mock 을 만든다**

`lib/features/card/data/card_repository.dart`:

```dart
import 'models/player_card.dart';

/// 화면이 아는 유일한 카드 계약.
///
/// 🔴 **「없음」을 예외로 던지지 않는다.** `GET /me/card` 의 404
/// `CARD_NOT_FOUND` 는 오류가 아니라 「아직 안 만들었다」이고, 그것을 예외로
/// 올리면 화면마다 try/catch 로 정상 상태를 가려내야 한다 — 그러면 진짜 오류
/// (네트워크·401)와 구분이 사라진다.
abstract class CardRepository {
  /// 내 카드. **아직 없으면 `null`** 이다.
  Future<PlayerCard?> myCard();

  /// 카드를 만든다. **멱등** — 이미 있으면 있는 것을 그대로 돌려준다.
  Future<PlayerCard> createMyCard();

  /// 남의 카드를 공개 슬러그로. 없으면 `null`. **인증하지 않는다.**
  Future<PlayerCard?> cardBySlug(String slug);
}
```

`lib/core/mock/mock_db.dart` 에 카드 자리를 만든다 — `final List<PlayerCard> cards = [];` 와 `_seed()` 안:

```dart
    // 🔴 **playerId 의 카드는 일부러 안 만든다.** 「카드 없음」 빈 상태를
    //    반드시 만들게 하는 장치다(newbieId 에 소속을 안 넣은 것과 같은 이유).
    cards.add(const PlayerCard(
      id: 'pc-manager',
      publicSlug: 'lee-gamdok-7f21',
      nickname: '이감독',
      tagline: 'THREE LUNGS',
    ));
```

`lib/features/card/data/card_repository_mock.dart`:

```dart
import 'dart:math' as math;

import '../../../core/mock/mock_db.dart';
import 'card_repository.dart';
import 'models/player_card.dart';

class MockCardRepository implements CardRepository {
  MockCardRepository(this._db, {required this.userId});

  final MockDb _db;

  /// 지금 로그인한 사람 — 「내 카드」가 누구 것인지 가른다.
  final String userId;

  /// 계약 테스트가 「있는 카드」로 쓰는 슬러그(시드의 이감독 카드).
  static const managerCardSlug = 'lee-gamdok-7f21';

  static const _delay = Duration(milliseconds: 300);

  @override
  Future<PlayerCard?> myCard() async {
    // 🔴 Mock 이 즉시 성공하면 로딩 UI 를 안 만들게 되고, API 를 붙이는 날
    //    화면을 다시 짠다. 다른 Mock 과 같은 지연을 쓴다.
    await Future<void>.delayed(_delay);
    return _mine;
  }

  @override
  Future<PlayerCard> createMyCard() async {
    await Future<void>.delayed(_delay);
    // 🔴 **멱등이다** — 이미 있으면 있는 것을 그대로 돌려준다. 새 슬러그를
    //    뽑으면 이미 공유한 주소가 죽는다(계약 POST /me/card).
    final existing = _mine;
    if (existing != null) return existing;
    final card = PlayerCard(
      id: 'pc-$userId',
      // 슬러그는 닉네임에서 유도하지 않는다(SEC-005) — 여기서도 같은 성질을
      // 흉내 내려고 난수를 붙인다.
      publicSlug: 'card-${math.Random().nextInt(0xFFFFFF).toRadixString(16)}',
      nickname: _db.findUserById(userId)?.nickname ?? '',
    );
    _db.cards.add(card);
    return card;
  }

  @override
  Future<PlayerCard?> cardBySlug(String slug) async {
    await Future<void>.delayed(_delay);
    for (final c in _db.cards) {
      if (c.publicSlug == slug) return c;
    }
    return null;
  }

  PlayerCard? _mine {
    for (final c in _db.cards) {
      if (c.id == 'pc-$userId') return c;
    }
    return null;
  }
}
```

🔴 `_mine` 은 getter 문법(`PlayerCard? get _mine { … }`)으로 쓴다.

- [ ] **Step 7: Mock 에 계약을 물린다**

`test/features/card/card_repository_mock_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/core/mock/mock_db.dart';
import 'package:super_sub/features/card/data/card_repository_mock.dart';

import '../../contract/card_repository_contract.dart';

void main() {
  runCardRepositoryContract(
    'MockCardRepository',
    () => MockCardRepository(MockDb(), userId: MockDb.playerId),
    slugWithCard: MockCardRepository.managerCardSlug,
  );

  // 구현체 전용 — 계약에 두면 API 가 통과할 수 없다.
  test('Mock 은 일부러 느리다', () async {
    final sw = Stopwatch()..start();
    await MockCardRepository(MockDb(), userId: MockDb.playerId).myCard();
    expect(sw.elapsedMilliseconds, greaterThanOrEqualTo(200));
  });
}
```

Run: `flutter test test/features/card/`
Expected: PASS

- [ ] **Step 8: API 구현체를 만들고 같은 계약을 물린다**

`card_repository_api.dart`:

```dart
class ApiCardRepository implements CardRepository {
  ApiCardRepository(this._api);

  final ApiClient _api;

  @override
  Future<PlayerCard?> myCard() => _orNullOn404(() => _api.get('/me/card'));

  @override
  Future<PlayerCard> createMyCard() async =>
      PlayerCard.fromJson(await _api.post('/me/card'));

  @override
  Future<PlayerCard?> cardBySlug(String slug) => _orNullOn404(
        // 🔴 인증하지 않는다 — 공개 경로다(슬러그 96비트 난수가 접근 통제, SEC-005).
        () => _api.get('/cards/${Uri.encodeComponent(slug)}', authorized: false),
      );

  /// 🔴 **404 만** null 로 바꾼다. 401·500 까지 삼키면 「로그인이 풀렸다」와
  /// 「카드가 없다」가 같아 보여, 화면이 빈 카드를 그리며 조용히 잘못된 상태에
  /// 머문다.
  Future<PlayerCard?> _orNullOn404(
    Future<Map<String, dynamic>> Function() request,
  ) async {
    try {
      return PlayerCard.fromJson(await request());
    } on ApiException catch (e) {
      if (e.status == 404) return null;
      rethrow;
    }
  }
}
```

`test/features/card/card_repository_api_test.dart` 는 `MockClient` 로 계약을 물린다 — 경로별로 응답을 갈라 주고(`/me/card` 는 처음 404, `POST` 뒤엔 200), `runCardRepositoryContract` 를 그대로 부른다.

- [ ] **Step 9: 교체 지점을 만든다**

`card_providers.dart`:

```dart
/// 🔴 백엔드 교체 지점 — 여기 한 줄이다. 화면·위젯은 수정하지 않는다.
final cardRepositoryProvider = Provider<CardRepository>((ref) {
  if (ref.watch(useMockProvider)) {
    return MockCardRepository(
      ref.watch(mockDbProvider),
      userId: MockDb.playerId,
    );
  }
  return ApiCardRepository(ref.watch(apiClientProvider));
});

/// 내 카드. **`null` 은 정상**이다 — 아직 안 만든 것이다.
///
/// retry 를 끈 이유는 `sportsProvider` 와 같다 — Riverpod 3 가 자동 재시도하면
/// 화면이 오류 대신 로딩만 계속 보여준다.
final myCardProvider = FutureProvider<PlayerCard?>(
  (ref) => ref.watch(cardRepositoryProvider).myCard(),
  retry: (_, _) => null,
);
```

- [ ] **Step 10: 전체를 돌리고 커밋한다**

Run: `flutter analyze && flutter test`

```bash
git add lib/features/card/ lib/core/mock/mock_db.dart test/contract/card_repository_contract.dart test/features/card/
git commit -m "feat(flutter): 카드 리포지토리 — GET/POST /me/card · GET /cards/{slug}"
```

---

### Task 4: 스쿼드 — 모델 · 리포지토리 · 계약 테스트 · 두 구현체

**Files:**
- Create: `lib/features/team/data/models/squad.dart`
- Create: `lib/features/team/data/squad_repository.dart`
- Create: `lib/features/team/data/squad_repository_mock.dart`
- Create: `lib/features/team/data/squad_repository_api.dart`
- Create: `lib/features/team/data/squad_providers.dart`
- Test: `test/contract/squad_repository_contract.dart`
- Test: `test/features/team/squad_repository_mock_test.dart`
- Test: `test/features/team/squad_repository_api_test.dart`
- Test: `test/features/team/squad_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `ApiClient`·`ApiException`
- Produces:
  - `class SquadMember { final String id, playerCardId, nickname, positionCode, positionLabel; final String? cardPublicSlug; final int? gridCol, gridRow; final bool accepted; bool get hasSeat; }`
    — 🔴 `accepted_at` 을 **`DateTime` 으로 들고 있지 않는다.** 화면이 쓰는 것은 「왔는가/대기중인가」 하나뿐이고, 시각을 그대로 들면 「없음」과 「null」을 가르는 규칙(아래 Step 3)이 읽는 쪽마다 흩어진다
  - `class Squad { final String id, teamId, publicSlug; final String? formation; final List<SquadMember> members; }`
  - `abstract class SquadRepository` — `Future<Squad?> squadOf(String teamId)`
  - `final squadRepositoryProvider`, `final squadProvider = FutureProvider.family<Squad?, String>(…)`

- [ ] **Step 1: 모델 테스트를 쓴다**

`test/features/team/squad_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/team/data/models/squad.dart';

void main() {
  test('계약 응답을 읽는다', () {
    final squad = Squad.fromJson(const {
      'id': '7c05',
      'team_id': '3f1c',
      'public_slug': 'aB3xK9mQ2pL7vN4t',
      'formation': '5:5',
      'members': [
        {
          'id': '1d4f',
          'player_card_id': '9a2e',
          'card_public_slug': 'hong-gildong-4f2a',
          'nickname': '홍길동',
          'position_code': 'GK',
          'position_label': '골키퍼',
          'grid_col': 1,
          'grid_row': 3,
        }
      ],
    });

    expect(squad.formation, '5:5');
    final m = squad.members.single;
    expect(m.positionCode, 'GK');
    expect(m.gridCol, 1);
    expect(m.gridRow, 3);
  });

  test('판에 안 올린 등재는 칸이 null 이다', () {
    final squad = Squad.fromJson(const {
      'id': '7c05', 'team_id': '3f1c', 'public_slug': 's',
      'formation': '5:5',
      'members': [
        {'id': '1', 'player_card_id': '9', 'card_public_slug': null,
         'nickname': '아무개', 'position_code': 'MF', 'position_label': '미드필더',
         'grid_col': null, 'grid_row': null},
      ],
    });

    expect(squad.members.single.gridCol, isNull);
    expect(squad.members.single.cardPublicSlug, isNull);
  });

  test('formation 이 null 일 수 있다 — 2026-09-18 이전 스쿼드', () {
    final squad = Squad.fromJson(const {
      'id': '7c05', 'team_id': '3f1c', 'public_slug': 's',
      'formation': null, 'members': [],
    });

    expect(squad.formation, isNull);
  });

  test('accepted_at 이 아예 안 오면 「수락됨」으로 본다', () {
    // 🔴 옛 응답에는 이 칸이 없다. 그 시절엔 팀원만 앉을 수 있어서 앉은 것이
    //    곧 온 것이었다 — 그래서 없으면 수락된 것으로 본다(웹과 같은 판단).
    final squad = Squad.fromJson(const {
      'id': '7c05', 'team_id': '3f1c', 'public_slug': 's', 'formation': '5:5',
      'members': [
        {'id': '1', 'player_card_id': '9', 'card_public_slug': 's1',
         'nickname': '아무개', 'position_code': 'MF', 'position_label': '미드필더',
         'grid_col': 0, 'grid_row': 1},
      ],
    });

    expect(squad.members.single.accepted, isTrue);
  });

  test('accepted_at 이 null 로 오면 대기중이다', () {
    final squad = Squad.fromJson(const {
      'id': '7c05', 'team_id': '3f1c', 'public_slug': 's', 'formation': '5:5',
      'members': [
        {'id': '1', 'player_card_id': '9', 'card_public_slug': 's1',
         'nickname': '아무개', 'position_code': 'MF', 'position_label': '미드필더',
         'grid_col': 0, 'grid_row': 1, 'accepted_at': null},
      ],
    });

    expect(squad.members.single.accepted, isFalse);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/team/squad_test.dart`
Expected: FAIL — 파일 없음

- [ ] **Step 3: 모델을 만든다**

`lib/features/team/data/models/squad.dart`:

```dart
/// 스쿼드에 등재된 한 명.
class SquadMember {
  const SquadMember({
    required this.id,
    required this.playerCardId,
    required this.nickname,
    required this.positionCode,
    required this.positionLabel,
    required this.accepted,
    this.cardPublicSlug,
    this.gridCol,
    this.gridRow,
  });

  factory SquadMember.fromJson(Map<String, dynamic> json) => SquadMember(
        id: json['id'] as String,
        playerCardId: json['player_card_id'] as String,
        // 🔴 내부 id 를 밖에 내보내지 않는 것이 카드와 같은 원칙이라, 남의
        //    카드로 가는 길은 이 슬러그뿐이다. `null` 도 정상이다.
        cardPublicSlug: json['card_public_slug'] as String?,
        nickname: json['nickname'] as String,
        positionCode: json['position_code'] as String,
        positionLabel: json['position_label'] as String,
        gridCol: json['grid_col'] as int?,
        gridRow: json['grid_row'] as int?,
        /* 🔴 **칸이 아예 없으면 수락된 것으로 본다.** 옛 응답에는 이 칸이
           없었고 그때는 팀원만 앉을 수 있어 앉은 것이 곧 온 것이었다. 새
           응답에서 `null` 로 **와 있으면** 그때는 정말 대기중이다.
           ⚠️ 이 필드는 계약 문서 3-7절에 아직 안 적혀 있다(계약 60으로
           들어왔다) — 정어진에게 문서 보강을 요청해 두었다. */
        accepted: !json.containsKey('accepted_at') || json['accepted_at'] != null,
      );

  final String id;
  final String playerCardId;
  final String? cardPublicSlug;
  final String nickname;
  final String positionCode;
  final String positionLabel;
  final int? gridCol;
  final int? gridRow;

  /// 이 사람이 **오기로 했는가**.
  final bool accepted;

  /// 판 칸에 올라가 있는가. 🔴 **둘은 함께 있거나 함께 없다**(계약).
  bool get hasSeat => gridCol != null && gridRow != null;
}

/// 팀 하나의 스쿼드. 경로가 `/teams/{id}/squad` **단수**이고 생성이 멱등이라
/// 애플리케이션은 팀당 하나로 다룬다.
class Squad {
  const Squad({
    required this.id,
    required this.teamId,
    required this.publicSlug,
    required this.members,
    this.formation,
  });

  factory Squad.fromJson(Map<String, dynamic> json) => Squad(
        id: json['id'] as String,
        teamId: json['team_id'] as String,
        publicSlug: json['public_slug'] as String,
        // ⚠️ **`null` 을 만날 수 있다** — 2026-09-18 이전에 만들어진 스쿼드다.
        //    읽는 쪽은 「모르는 값이면 기본 판으로 연다」를 지킨다.
        formation: json['formation'] as String?,
        members: ((json['members'] as List<dynamic>?) ?? const [])
            .map((e) => SquadMember.fromJson(e as Map<String, dynamic>))
            .toList(growable: false),
      );

  final String id;
  final String teamId;
  final String publicSlug;
  final String? formation;
  final List<SquadMember> members;
}
```

- [ ] **Step 4: 모델 테스트 통과를 확인한다**

Run: `flutter test test/features/team/squad_test.dart`
Expected: PASS (5개)

- [ ] **Step 5: 계약 테스트 · 인터페이스 · 두 구현체**

`test/contract/squad_repository_contract.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/team/data/squad_repository.dart';

/// SquadRepository 의 모든 구현체가 지켜야 하는 계약.
///
/// [teamWithSquad] 는 스쿼드가 **있는** 팀, [teamWithoutSquad] 는 팀은 있으나
/// 스쿼드를 **아직 안 만든** 팀이다.
void runSquadRepositoryContract(
  String name,
  SquadRepository Function() build, {
  required String teamWithSquad,
  required String teamWithoutSquad,
}) {
  group('$name — SquadRepository 계약', () {
    late SquadRepository repo;

    setUp(() => repo = build());

    test('스쿼드를 읽는다', () async {
      final squad = await repo.squadOf(teamWithSquad);
      expect(squad, isNotNull);
      expect(squad!.teamId, equals(teamWithSquad));
    });

    test('아직 안 만든 스쿼드는 예외가 아니라 null 이다', () async {
      // 🔴 빈 스쿼드를 돌려주면 「만들지 않은 것」과 「비어 있는 것」이 같아
      //    보인다 — 계약이 404 SQUAD_NOT_FOUND 를 내는 이유가 그것이다.
      expect(await repo.squadOf(teamWithoutSquad), isNull);
    });
  });
}
```

`lib/features/team/data/squad_repository.dart`:

```dart
import 'models/squad.dart';

/// 화면이 아는 유일한 스쿼드 계약.
abstract class SquadRepository {
  /// 팀의 스쿼드. **아직 안 만들었으면 `null`** 이다 — 빈 스쿼드를 돌려주면
  /// 「만들지 않은 것」과 「비어 있는 것」이 같아 보인다(계약 3-7절).
  Future<Squad?> squadOf(String teamId);
}
```

`squad_repository_mock.dart` — `MockDb` 에 `final List<Squad> squads = [];` 를 두고 `t-thunder` 것만 시드한다. 🔴 **`t-bears` 에는 두지 않는다**(「안 만든 팀」 갈래를 반드시 만들게 한다).

```dart
class MockSquadRepository implements SquadRepository {
  MockSquadRepository(this._db);

  final MockDb _db;
  static const _delay = Duration(milliseconds: 300);

  @override
  Future<Squad?> squadOf(String teamId) async {
    await Future<void>.delayed(_delay);
    for (final s in _db.squads) {
      if (s.teamId == teamId) return s;
    }
    return null;
  }
}
```

`MockDb._seed()` 에 더한다 — 칸이 있는 등재 하나와 **칸이 없는 등재 하나**를 둔다(2단계 갈래를 Mock 에서도 밟게 한다):

```dart
    squads.add(const Squad(
      id: 'sq-thunder',
      teamId: 't-thunder',
      publicSlug: 'aB3xK9mQ2pL7vN4t',
      formation: '5:5',
      members: [
        SquadMember(
          id: 'sm-1', playerCardId: 'pc-manager',
          cardPublicSlug: 'lee-gamdok-7f21', nickname: '이감독',
          positionCode: 'GK', positionLabel: '골키퍼',
          gridCol: 1, gridRow: 3, accepted: true,
        ),
        // 🔴 칸이 없는 등재 — 판에 안 올린 사람도 포지션으로 앉는다.
        SquadMember(
          id: 'sm-2', playerCardId: 'pc-x',
          cardPublicSlug: null, nickname: '박신입',
          positionCode: 'MF', positionLabel: '미드필더',
          accepted: true,
        ),
      ],
    ));
```

`squad_repository_api.dart` — Task 3 의 `_orNullOn404` 와 같은 모양이다:

```dart
class ApiSquadRepository implements SquadRepository {
  ApiSquadRepository(this._api);

  final ApiClient _api;

  @override
  Future<Squad?> squadOf(String teamId) async {
    try {
      return Squad.fromJson(
        await _api.get('/teams/${Uri.encodeComponent(teamId)}/squad'),
      );
    } on ApiException catch (e) {
      // 🔴 404 만 null 로 바꾼다. 401·403 까지 삼키면 「소속이 아니라 못 본다」와
      //    「아직 안 만들었다」가 같아 보인다.
      if (e.status == 404) return null;
      rethrow;
    }
  }
}
```

`squad_providers.dart`:

```dart
/// 🔴 백엔드 교체 지점. `sport_providers.dart` 와 **따로 둔다** — 한 파일에
/// 모으면 종목을 고칠 때 스쿼드가 함께 다시 만들어진다.
final squadRepositoryProvider = Provider<SquadRepository>((ref) {
  if (ref.watch(useMockProvider)) {
    return MockSquadRepository(ref.watch(mockDbProvider));
  }
  return ApiSquadRepository(ref.watch(apiClientProvider));
});

/// 팀 하나의 스쿼드. **`null` 은 정상** — 아직 안 만든 것이다.
final squadProvider = FutureProvider.family<Squad?, String>(
  (ref, teamId) => ref.watch(squadRepositoryProvider).squadOf(teamId),
  retry: (_, _) => null,
);
```

- [ ] **Step 6: 통과를 확인하고 커밋한다**

Run: `flutter analyze && flutter test`

```bash
git add lib/features/team/ test/contract/squad_repository_contract.dart test/features/team/
git commit -m "feat(flutter): 스쿼드 리포지토리 — GET /teams/{id}/squad"
```

---

### Task 5: `seatsFromSquad` — 자리 배치 순수 함수

웹 `SquadPanel.tsx:162-281` 을 옮긴다. 🔴 **재해석하지 않는다.** 세 단계 각각이 실제 사고에서 나왔고, 줄이면 그 사고가 돌아온다.

**Files:**
- Create: `lib/features/team/seats_from_squad.dart`
- Modify: `lib/features/team/presentation/widgets/squad_board.dart` (`SquadSlot`·`kFormations` 를 이 파일로 옮기고 re-export — 배치 규칙과 포메이션이 한곳에 있어야 한다)
- Test: `test/features/team/seats_from_squad_test.dart`

**Interfaces:**
- Consumes: Task 4 의 `Squad`·`SquadMember`, 기존 `SquadSlot`·`SquadSize`·`kFormations`
- Produces:
  - `class SeatAssignment { final List<SquadSlot> slots; final Map<String, String> mates; final Map<String, String?> slugs; final Map<String, String> memberIds; final Map<String, bool> ready; }`
  - `SeatAssignment seatsFromSquad(Squad? squad, SquadSize size, {String? mySlug})`
  - `SquadSize squadSizeOf(String? formation)` — `'3:3'`→three, `'7:7'`→seven, **그 밖·null → five**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test/features/team/seats_from_squad_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:super_sub/features/team/data/models/squad.dart';
import 'package:super_sub/features/team/seats_from_squad.dart';

SquadMember _m({
  required String id,
  required String slug,
  required String nickname,
  required String pos,
  int? col,
  int? row,
  bool accepted = true,
}) =>
    SquadMember(
      id: id,
      playerCardId: 'card-$id',
      cardPublicSlug: slug,
      nickname: nickname,
      positionCode: pos,
      positionLabel: pos,
      gridCol: col,
      gridRow: row,
      accepted: accepted,
    );

Squad _squad(List<SquadMember> members, {String? formation = '5:5'}) => Squad(
      id: 's', teamId: 't', publicSlug: 'p',
      formation: formation, members: members,
    );

void main() {
  test('스쿼드가 없으면 빈 포메이션 그대로다', () {
    final r = seatsFromSquad(null, SquadSize.five);
    expect(r.slots, hasLength(5));
    expect(r.mates, isEmpty);
    expect(r.slots.any((s) => s.mine), isFalse);
  });

  test('내 등재가 없으면 판에 내 카드가 없다', () {
    // 🔴 웹이 2026-09-16 에 뒤집은 규칙 — 내 자리는 포메이션에 박혀 있지 않다.
    //    등재하지 않으면 판에 없다(「나는 안 뛴다」가 그렇게 표현된다).
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 'other', nickname: '남', pos: 'GK', col: 1, row: 3)]),
      SquadSize.five,
      mySlug: 'mine',
    );
    expect(r.slots.any((s) => s.mine), isFalse);
  });

  test('내 등재는 내가 앉힌 칸에 선다', () {
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 'mine', nickname: '나', pos: 'DF', col: 1, row: 2)]),
      SquadSize.five,
      mySlug: 'mine',
    );
    final mine = r.slots.singleWhere((s) => s.mine);
    expect(mine.col, 1);
    expect(mine.row, 2);
  });

  test('내 자리에는 이름표를 안 붙인다', () {
    // 🔴 mates 에 넣으면 판이 그 자리를 「남이 앉은 카드」로 그려서 내 카드
    //    대신 이름표가 선다.
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 'mine', nickname: '나', pos: 'DF', col: 1, row: 2)]),
      SquadSize.five,
      mySlug: 'mine',
    );
    expect(r.mates.values, isNot(contains('나')));
  });

  test('칸이 저장된 등재는 그 칸에 앉는다', () {
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 's1', nickname: '김철수', pos: 'MF', col: 0, row: 1)]),
      SquadSize.five,
    );
    final seat = r.slots.singleWhere((s) => r.mates[s.area] == '김철수');
    expect(seat.col, 0);
    expect(seat.row, 1);
    expect(r.slugs[seat.area], 's1');
  });

  test('같은 칸에 둘이면 뒤엣것을 건너뛴다', () {
    final r = seatsFromSquad(
      _squad([
        _m(id: '1', slug: 's1', nickname: '먼저', pos: 'MF', col: 0, row: 1),
        _m(id: '2', slug: 's2', nickname: '나중', pos: 'MF', col: 0, row: 1),
      ]),
      SquadSize.five,
    );
    expect(r.mates.values, contains('먼저'));
    expect(r.mates.values, isNot(contains('나중')));
  });

  test('칸이 없는 등재는 포지션이 맞는 빈 자리에 앉는다', () {
    // ⚠️ 계약대로면 「판에 안 올린 등재」라 안 그리는 것이 맞지만, 서버의 기존
    //    행이 전부 null 이라 그러면 판이 통째로 비어 보인다 — 등재된 사람이
    //    화면에서 사라지는 쪽이 더 나쁘다. 🔴 단 여기서 저장하지는 않는다.
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 's1', nickname: '이영희', pos: 'GK')]),
      SquadSize.five,
    );
    final seat = r.slots.singleWhere((s) => r.mates[s.area] == '이영희');
    expect(seat.position, 'GK');
  });

  test('칸이 없는 내 등재도 앉는다 — 여기서 me 를 빼면 안 된다', () {
    /* 🔴 2026-09-18 에 웹이 데인 것. 0단계는 **칸이 있는** 내 등재만 잡고
       1단계는 me 를 건너뛰므로, 여기서까지 건너뛰면 칸 없는 내 등재는 세
       단계 어디에도 안 걸려 판에서 사라진다. 계약 60이 「수락하면 칸은 null
       로 등재」를 만든 뒤로는 **수락해서 들어온 사람 전부**가 자기 판에서
       자기를 못 보게 됐다. */
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 'mine', nickname: '나', pos: 'FW')]),
      SquadSize.five,
      mySlug: 'mine',
    );
    expect(r.slots.any((s) => s.mine), isTrue);
    expect(r.mates.values, isNot(contains('나')));
  });

  test('수락 대기중인 사람이 표시된다', () {
    final r = seatsFromSquad(
      _squad([_m(id: '1', slug: 's1', nickname: '대기', pos: 'MF', col: 0, row: 1, accepted: false)]),
      SquadSize.five,
    );
    final seat = r.slots.singleWhere((s) => r.mates[s.area] == '대기');
    expect(r.ready[seat.area], isFalse);
  });

  test('포메이션 상수를 더럽히지 않는다', () {
    // 🔴 kFormations 는 모듈 상수다. 자리를 제자리에서 고치면 다음 호출이
    //    남의 배치를 물려받는다.
    seatsFromSquad(
      _squad([_m(id: '1', slug: 's1', nickname: '김', pos: 'MF', col: 2, row: 0)]),
      SquadSize.five,
    );
    final fresh = seatsFromSquad(null, SquadSize.five);
    expect(fresh.slots.map((s) => '${s.col},${s.row}').toList(),
        equals(kFormations[SquadSize.five]!.map((s) => '${s.col},${s.row}').toList()));
  });

  group('squadSizeOf', () {
    test('아는 값을 옮긴다', () {
      expect(squadSizeOf('3:3'), SquadSize.three);
      expect(squadSizeOf('7:7'), SquadSize.seven);
    });

    test('모르는 값·null 은 기본 판(5:5)이다', () {
      // ⚠️ 2026-09-18 이전 스쿼드가 전부 null 이다.
      expect(squadSizeOf(null), SquadSize.five);
      expect(squadSizeOf('11:11'), SquadSize.five);
    });
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/team/seats_from_squad_test.dart`
Expected: FAIL — `seats_from_squad.dart` 없음

- [ ] **Step 3: 순수 함수를 만든다**

`lib/features/team/seats_from_squad.dart` — `SquadSlot`·`SquadSize`·`kFormations` 를 `squad_board.dart` 에서 **이 파일로 옮기고**, `SquadSlot.mine` 을 `final` 에서 **바꿀 수 있게**(`copyWith` 를 쓰거나 가변 필드로) 한다. 🔴 `kFormations` 의 `mine: true` 를 **전부 지운다.**

```dart
/// 자리 배치 결과. 키는 전부 `SquadSlot.area`(`fw1`·`mf2` …)다.
class SeatAssignment {
  const SeatAssignment({
    required this.slots,
    required this.mates,
    required this.slugs,
    required this.memberIds,
    required this.ready,
  });

  final List<SquadSlot> slots;

  /// 그 자리에 앉은 **남**의 이름. 🔴 내 자리는 여기 없다.
  final Map<String, String> mates;

  /// 그 자리 사람의 카드 공개 슬러그 — 진짜 카드를 그리려면 필요하다.
  final Map<String, String?> slugs;
  final Map<String, String> memberIds;

  /// 그 자리의 사람이 **오기로 했는가**.
  final Map<String, bool> ready;
}

/// 서버 `formation` → 판 크기. 🔴 **모르는 값이면 기본 판**이다.
SquadSize squadSizeOf(String? formation) => switch (formation) {
      '3:3' => SquadSize.three,
      '7:7' => SquadSize.seven,
      _ => SquadSize.five,
    };

/// 서버 스쿼드를 판 위의 자리로 — 웹 `SquadPanel.tsx` 의 `seatsFromSquad` 를
/// 그대로 옮긴 것이다. 🔴 **세 단계의 순서가 곧 규칙이다**(설계 문서 2-2).
SeatAssignment seatsFromSquad(
  Squad? squad,
  SquadSize size, {
  /// 내 카드의 공개 슬러그 — **어느 등재가 나인지** 가리는 열쇠다.
  String? mySlug,
}) {
  // 🔴 자리는 **복사해서** 만진다. kFormations 는 모듈 상수라 제자리에서
  //    고치면 다음 호출이 남의 배치를 물려받는다.
  final slots = kFormations[size]!.map((s) => s.copy()).toList();
  final mates = <String, String>{};
  final slugs = <String, String?>{};
  final memberIds = <String, String>{};
  final ready = <String, bool>{};
  if (squad == null) {
    return SeatAssignment(
      slots: slots, mates: mates, slugs: slugs,
      memberIds: memberIds, ready: ready,
    );
  }

  bool taken(SquadSlot s) => s.mine || mates.containsKey(s.area);

  // 0) 내 등재를 먼저 집는다. 칸이 있으면 그 칸에.
  SquadMember? me;
  if (mySlug != null) {
    for (final m in squad.members) {
      if (m.cardPublicSlug == mySlug) {
        me = m;
        break;
      }
    }
  }
  if (me != null && me.hasSeat) {
    for (final s in slots) {
      if (s.col == me.gridCol && s.row == me.gridRow) {
        s.mine = true;
        memberIds[s.area] = me.id;
        s.positionCode = me.positionCode;
        break;
      }
    }
  }

  // 1) 칸이 저장된 등재 — 그 칸이 곧 자리다.
  for (final m in squad.members) {
    if (identical(m, me) || !m.hasSeat) continue;
    // 🔴 그 칸이 이미 찼으면 건너뛴다 — 같은 사람이 두 번 나오지 않게.
    if (slots.any((s) => taken(s) && s.col == m.gridCol && s.row == m.gridRow)) {
      continue;
    }
    /* 🔴 **그 칸에 있는 자리를 먼저 쓴다.** 아무 빈 자리나 끌어다 옮기면
       자리들이 통째로 뒤엉켜, 원래 그 칸에 있던 자리가 밀려나며 카드가 겹쳐
       사라진다. 그 칸에 자리가 없을 때만(골키퍼 줄 양옆처럼 아예 없는 칸,
       또는 판이 작아진 경우) 남는 자리를 옮겨 온다. */
    final seat = slots.cast<SquadSlot?>().firstWhere(
              (s) => !taken(s!) && s.col == m.gridCol && s.row == m.gridRow,
              orElse: () => null,
            ) ??
        slots.cast<SquadSlot?>().firstWhere((s) => !taken(s!), orElse: () => null);
    if (seat == null) continue;
    seat.col = m.gridCol!;
    seat.row = m.gridRow!;
    seat.positionCode = m.positionCode;
    mates[seat.area] = m.nickname;
    slugs[seat.area] = m.cardPublicSlug;
    memberIds[seat.area] = m.id;
    ready[seat.area] = m.accepted;
  }

  // 2) 칸이 없는 등재는 포지션이 맞는 빈 자리에.
  for (final m in squad.members) {
    if (m.hasSeat) continue;
    final seat = slots.cast<SquadSlot?>().firstWhere(
          (s) => !taken(s!) && s.position == m.positionCode,
          orElse: () => null,
        );
    if (seat == null) continue;
    // 🔴 여기서 me 를 빼면 안 된다 — 빼면 수락해서 들어온 사람 전부가 자기
    //    판에서 자기를 못 본다(2026-09-18 웹이 데인 것).
    if (identical(m, me)) {
      seat.mine = true;
      memberIds[seat.area] = m.id;
      seat.positionCode = m.positionCode;
      continue;
    }
    mates[seat.area] = m.nickname;
    slugs[seat.area] = m.cardPublicSlug;
    memberIds[seat.area] = m.id;
    ready[seat.area] = m.accepted;
  }

  return SeatAssignment(
    slots: slots, mates: mates, slugs: slugs,
    memberIds: memberIds, ready: ready,
  );
}
```

`SquadSlot` 은 `copy()` 와 가변 `col`·`row`·`mine`·`positionCode` 를 갖게 고친다. `position` getter 는 🔴 **저장된 `positionCode` 가 있으면 그것을, 없으면 행에서** 온다 — 손으로 정했을 수 있어 늘 행에서 역산하면 안 된다:

```dart
  String get position => positionCode ?? const ['FW', 'MF', 'DF', 'GK'][row];
```

- [ ] **Step 4: 통과를 확인한다**

Run: `flutter test test/features/team/seats_from_squad_test.dart`
Expected: PASS (13개)

- [ ] **Step 5: 커밋한다**

Run: `flutter analyze && flutter test`

```bash
git add lib/features/team/ test/features/team/seats_from_squad_test.dart
git commit -m "feat(flutter): 자리 배치를 순수 함수로 — 웹 seatsFromSquad 3단계를 그대로"
```

---

### Task 6: `SquadBoard` 가 서버 스쿼드를 그린다

**Files:**
- Modify: `lib/features/team/presentation/widgets/squad_board.dart`
- Test: `test/features/team/squad_board_test.dart`

**Interfaces:**
- Consumes: Task 5 의 `seatsFromSquad`·`squadSizeOf`·`SeatAssignment`, Task 4 의 `Squad`
- Produces: `SquadBoard({required String? cardSeed, required Squad? squad, required String? mySlug, required ValueChanged<SquadSlot> onSeatTap, Widget Function(String slug)? mateCardBuilder})`

- [ ] **Step 1: 실패하는 위젯 테스트를 쓴다**

`test/features/team/squad_board_test.dart` — 판은 무한 애니메이션이 없으므로 `pump` 로 충분하다.

```dart
  testWidgets('서버 스쿼드의 사람이 판에 뜬다', (tester) async {
    await tester.pumpWidget(_wrap(SquadBoard(
      cardSeed: 'mine',
      mySlug: 'mine',
      squad: _squadWith(nickname: '김철수', pos: 'MF', col: 0, row: 1),
      onSeatTap: (_) {},
    )));

    expect(find.text('김철수'), findsOneWidget);
  });

  testWidgets('formation 이 서버 값으로 열린다', (tester) async {
    await tester.pumpWidget(_wrap(SquadBoard(
      cardSeed: 'mine', mySlug: 'mine',
      squad: _emptySquad(formation: '3:3'),
      onSeatTap: (_) {},
    )));

    // 3:3 은 자리가 셋이다.
    expect(find.byKey(const ValueKey('squad-seat-gk')), findsOneWidget);
    expect(find.byKey(const ValueKey('squad-seat-df1')), findsNothing);
  });

  testWidgets('등재하지 않았으면 내 카드가 판에 없다', (tester) async {
    await tester.pumpWidget(_wrap(SquadBoard(
      cardSeed: 'mine', mySlug: 'mine',
      squad: _emptySquad(), onSeatTap: (_) {},
    )));

    // 자리가 전부 빈 카드(+)다.
    expect(find.byIcon(Icons.add), findsNWidgets(5));
  });
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/team/squad_board_test.dart`
Expected: FAIL — `SquadBoard` 에 `squad`·`mySlug` 인자가 없다

- [ ] **Step 3: `SquadBoard` 를 고친다**

머리말의 「아직 안 옮긴 것」에서 **서버 스쿼드 줄을 지운다**(이제 옮겼다).

```dart
class _SquadBoardState extends State<SquadBoard> {
  /// 🔴 **사용자가 알약으로 고른 크기가 서버 값을 이긴다.** 안 그러면 판을
  /// 3:3 으로 바꾼 직후 스쿼드가 다시 도착하면서 5:5 로 되돌아간다.
  SquadSize? _picked;

  SquadSize get _size => _picked ?? squadSizeOf(widget.squad?.formation);

  @override
  Widget build(BuildContext context) {
    // 🔴 자리 계산은 순수 함수 한 곳이다 — 판이 스스로 배치를 정하면 웹과
    //    갈린다(`seats_from_squad.dart`).
    final seats = seatsFromSquad(widget.squad, _size, mySlug: widget.mySlug);

    return LayoutBuilder(
      builder: (context, box) {
        // … 기존 cellW · cellH · cardW · cardH 계산 그대로 …
        return Stack(
          children: [
            Positioned.fill(child: CustomPaint(painter: _PitchPainter())),
            Positioned(top: 12, left: 16, right: 12, child: _head()),
            for (final slot in seats.slots)
              Positioned(
                key: ValueKey('squad-seat-${slot.area}'),
                left: _padSide + slot.col * cellW + (cellW - cardW) / 2,
                top: _padTop +
                    slot.row * cellH +
                    (cellH - cardH - _labelGap - _labelH) / 2,
                width: cardW,
                child: _seat(slot, cardW, seats),
              ),
          ],
        );
      },
    );
  }

  /// 자리는 셋으로 갈린다 — 내 카드 · 남의 카드 · 빈 자리.
  Widget _seat(SquadSlot slot, double cardW, SeatAssignment seats) {
    final Widget card;
    if (slot.mine && widget.cardSeed != null) {
      card = PlayerCardView(width: cardW, seed: widget.cardSeed!);
    } else if (seats.mates.containsKey(slot.area)) {
      final slug = seats.slugs[slot.area];
      final mate = slug == null ? null : widget.mateCardBuilder?.call(slug);
      // 🔴 카드가 아직 안 왔거나 슬러그가 없으면 **이름표로 물러난다.**
      //    판 전체를 로딩으로 덮지 않는다 — 나머지 자리는 이미 그릴 수 있다.
      card = mate ??
          BlankPlayerCardView(
            width: cardW,
            child: Opacity(
              // 수락 대기중이면 흐리게 — 「아직 안 온 사람」이다.
              opacity: (seats.ready[slot.area] ?? true) ? 1 : 0.45,
              child: Text(
                seats.mates[slot.area]!,
                textAlign: TextAlign.center,
                style: const TextStyle(
                  fontSize: 40, fontWeight: FontWeight.w700, color: _kInk,
                ),
              ),
            ),
          );
    } else {
      card = GestureDetector(
        key: Key('squad-add-${slot.area}'),
        onTap: () => widget.onSeatTap(slot),
        child: BlankPlayerCardView(
          width: cardW,
          child: const Icon(Icons.add, size: 150, color: _kInk),
        ),
      );
    }
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        card,
        const SizedBox(height: _labelGap),
        SizedBox(
          height: _labelH,
          child: Text(slot.position, style: /* 기존 스타일 그대로 */),
        ),
      ],
    );
  }
}
```

`_head()` 의 알약은 `onTap: () => setState(() => _picked = size)` 로 바꾼다(`_size` 는 이제 getter 다).

🔴 **이름표 글자 크기 40 은 카드 제 크기(380 폭) 기준이다** — `BlankPlayerCardView` 안은 `FittedBox` 로 통째로 줄어들므로 여기에 화면 픽셀을 넣으면 작은 판에서 글자만 커진다.

- [ ] **Step 4: 통과를 확인한다**

Run: `flutter test test/features/team/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
git add lib/features/team/presentation/widgets/squad_board.dart test/features/team/squad_board_test.dart
git commit -m "feat(flutter): 스쿼드 판이 서버 데이터를 그린다"
```

---

### Task 7: 홈 배선 — 내 카드 씨앗 · 팀원 카드 지연 로딩

**Files:**
- Modify: `lib/features/home/presentation/screens/home_screen.dart` (274~290, 458, 580, 1193 근처)
- Create: `lib/features/card/presentation/mate_cards_controller.dart`
- Test: `test/features/home/home_screen_test.dart` (기존에 더한다)
- Test: `test/features/card/mate_cards_controller_test.dart`

**Interfaces:**
- Consumes: Task 3 의 `myCardProvider`·`cardRepositoryProvider`, Task 4 의 `squadProvider`, Task 2 의 `AppUser.primaryTeamId`
- Produces: `final mateCardsProvider = NotifierProvider<MateCardsController, Map<String, PlayerCard>>(…)` · `MateCardsController.want(Iterable<String> slugs)`

- [ ] **Step 1: 지연 로딩 테스트를 쓴다**

```dart
  test('같은 슬러그를 두 번 받지 않는다', () async {
    var calls = 0;
    // repo 대역이 부를 때마다 calls++
    controller.want(['s1', 's1', 's2']);
    await pumpEventQueue();
    controller.want(['s1', 's2']);
    await pumpEventQueue();

    expect(calls, 2);
  });

  test('카드 모양이 아닌 응답은 버린다', () async {
    // 🔴 없는 값이 카드 자리에 들어가면 카드가 통째로 깨진다.
    controller.want(['broken']);
    await pumpEventQueue();

    expect(controller.state.containsKey('broken'), isFalse);
  });
```

- [ ] **Step 2: 실패를 확인한다**

Run: `flutter test test/features/card/mate_cards_controller_test.dart`
Expected: FAIL — 파일 없음

- [ ] **Step 3: 컨트롤러를 만든다**

```dart
/// 팀원 카드 — **보이는 것만, 한 번만** 받는다.
///
/// 🔴 **목록을 그릴 때 줄마다 부르지 않는다.** 자리마다 카드를 부르면 판 하나를
/// 여는 데 요청이 자리 수만큼 나간다(웹 `SquadPanel` 이 같은 규칙을 쓴다).
class MateCardsController extends Notifier<Map<String, PlayerCard>> {
  final _inFlight = <String>{};

  @override
  Map<String, PlayerCard> build() => const {};

  void want(Iterable<String> slugs) {
    final repo = ref.read(cardRepositoryProvider);
    for (final slug in slugs.toSet()) {
      if (state.containsKey(slug) || _inFlight.contains(slug)) continue;
      _inFlight.add(slug);
      repo.cardBySlug(slug).then((card) {
        _inFlight.remove(slug);
        // 🔴 응답이 카드 모양이 아니면 버린다 — 없는 값을 읽다가 카드 자리가
        //    통째로 깨진다(웹이 같은 검사를 한다).
        if (card == null || card.publicSlug.isEmpty) return;
        state = {...state, slug: card};
      }).catchError((_) {
        // 한 장이 안 와도 판 전체를 오류로 만들지 않는다 — 그 자리만 이름표다.
        _inFlight.remove(slug);
      });
    }
  }
}
```

- [ ] **Step 4: 홈을 잇는다**

`home_screen.dart`:

```dart
    final session = ref.watch(sessionControllerProvider);
    final user = session is SessionLoggedIn ? session.user : null;
    final card = ref.watch(myCardProvider).valueOrNull;
    final teamId = user?.primaryTeamId;
    final squad = teamId == null
        ? null
        : ref.watch(squadProvider(teamId)).valueOrNull;

    // 🔴 붓자국 씨앗은 **카드의 공개 슬러그**다 — 이래야 웹과 같은 무늬가
    //    나온다. 카드가 없으면 `null` 이고 판은 빈 카드를 그린다.
    final cardSeed = card?.publicSlug;
```

그리고 `mateCardsProvider` 에 `seatsFromSquad` 가 준 슬러그를 넘긴다(빌드 중에 상태를 바꾸지 않도록 `WidgetsBinding.instance.addPostFrameCallback` 안에서 `want()` 를 부른다).

`_ProfileButton(seed: cardSeed)` 와 `SquadBoard(cardSeed: cardSeed, squad: squad, mySlug: card?.publicSlug, …)` 로 넘긴다. 274~279 의 「⚠️ 카드 서버 연결 전」 주석을 **지운다**.

- [ ] **Step 5: 위젯 테스트로 확인한다**

`home_screen_test.dart` 에 더한다:

```dart
  testWidgets('카드가 없으면 빈 카드를 그리고 터지지 않는다', (tester) async {
    // myCardProvider 를 null 로 덮어씌운다.
    await tester.pumpWidget(_home(card: null));
    await tester.pump(const Duration(milliseconds: 500));

    expect(tester.takeException(), isNull);
  });
```

Run: `flutter test test/features/home/`
Expected: PASS. 🔴 무한 애니메이션이 있으므로 `pumpAndSettle` 을 쓰지 않는다

- [ ] **Step 6: 전체 확인 · 인프라 식별자 검사 · 커밋**

Run: `flutter analyze && flutter test`

```bash
git diff --cached --name-only -- . | xargs grep -nE 'arn:aws:[a-z]+::[0-9]{12}|[0-9]{4}-[0-9]{4}-[0-9]{4}|(vpc|subnet|sg|eipalloc)-[0-9a-f]{8,}|\bi-0[0-9a-f]{16}\b|\b(3|13|15|43|52|54)\.[0-9]{1,3}\.[0-9]{1,3}\.[0-9]{1,3}\b|api\.[a-z0-9-]+\.[a-z]+' 2>/dev/null
```
Expected: 결과 없음

```bash
git add lib/features/home/ lib/features/card/ test/
git commit -m "feat(flutter): 홈이 내 카드·스쿼드를 서버에서 받는다 — 붓자국 씨앗도 슬러그로"
```

---

### Task 8: 실기기 확인 · 문서 · 미결 항목

**Files:**
- Modify: `flutter/docs/작업일지.md` (이번 회차)
- Modify: `jekyll/pages/pending.markdown` (`## paik` 구역에 새 항목 둘)

- [ ] **Step 1: 실기기에서 돌린다**

```bash
flutter run --dart-define-from-file=.env.local
```

🔴 기기(SM-A366N)가 adb 에 안 잡히면 `ADB_LIBUSB=1` 로 adb 서버를 다시 띄운다.

- [ ] **Step 2: 눈으로 확인할 것 넷**

| | 무엇 |
|---|---|
| 1 | 실제 계정으로 로그인 → 홈 판에 **웹에서 만든 팀원**이 뜬다 |
| 2 | 내 카드가 **내가 앉힌 칸**에 있다(안 앉혔으면 판에 없다) |
| 3 | 붓자국 무늬가 **웹과 같다**(같은 계정의 웹 카드와 나란히 본다) |
| 4 | 카드가 없는 계정에서 **빈 카드**가 뜨고 안 터진다 |

🔴 확인은 **adb 캡처까지**만 한다 — 기기를 대신 누르지 않는다.

- [ ] **Step 3: 미결 항목 둘을 올린다**

`jekyll/pages/pending.markdown` 의 `## paik (백성검)` 구역 **끝 번호 다음**에 `###` 로 둘을 더한다(🔴 남의 구역은 건드리지 않는다):

1. **「스쿼드 등재의 `accepted_at` 이 계약 문서에 없습니다」** — 담당 정어진 · 제기 백성검. 웹·앱 둘 다 읽고 있는데 3-7절 응답 예시에 없다. **코드 변경 요청이 아니라 문서 보강**이다. 확인: `grep -n 'accepted_at' fastapi/docs/api-contract.md` 가 3-7절에서도 걸리면 해소
2. **「카드 `mode: 'full'` 이 카드 폭 전체를 안 덮습니다」** — 담당 백성검(웹) · 제기 백성검. 주석은 「카드 전체를 덮는다」인데 좌우 16%가 안 풀려 폭의 68% 띠로 그려진다. 앱은 **웹과 픽셀 동일**로 옮겼으므로, 웹을 고치면 앱도 같이 고친다

- [ ] **Step 4: 작업일지를 쓰고 커밋한다**

```bash
git add flutter/docs/ jekyll/pages/pending.markdown
git commit -m "docs(flutter): 카드·스쿼드 서버 연결 1단계 — 작업일지 · 미결 둘"
```

---

## 완료 기준

- `flutter analyze` 0건 · `flutter test` 전부 통과
- 계약 테스트가 **Mock·API 양쪽**에서 돈다
- 실기기에서 위 「확인할 것 넷」이 전부 참이다
- 🔴 `grep -rn 'Mock[A-Za-z]*Repository(' lib/features/*/presentation lib/core/widgets` 가 **0건**이다 — 화면에서 구현체를 직접 만들면 「provider 한 줄 교체」가 깨진다
