"""컨텍스트에 속하지 않는 공용 모듈.

**여기 있는 것은 어느 컨텍스트에서든 임포트해도 된다.** 반대로 이 안에서
`app.user`·`app.card` 를 임포트하면 안 된다 — 그러면 공용이 아니다.
`tests/test_architecture.py` 가 그 방향을 검사한다.

공용인지 판단하는 기준: **둘 이상의 컨텍스트가 실제로 쓴다.** 하나만 쓰는 것은
그 컨텍스트 안에 둔다.
"""
